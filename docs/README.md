# docs/

Hier steht, **was** gebaut wird und **warum**. Wie gebaut wird, steht in
[`../DEVELOPMENT_GUIDELINES.md`](../DEVELOPMENT_GUIDELINES.md).

## Wie dieses Verzeichnis funktioniert

**Jedes Feature bekommt eine eigene Datei.** Keine Sammeldatei, keine „sonstiges.md".

| Anlass | Was zu tun ist |
|---|---|
| Neues Feature | **Neue Datei** anlegen und unten im Verzeichnis eintragen |
| Geändertes Feature | Die **vorhandene Datei aktualisieren** — nicht eine zweite anlegen |
| Entferntes Feature | Datei löschen, Eintrag entfernen |

**Ein Schritt gilt erst als fertig, wenn die Dokumentation steht.** Code ohne zugehörige
Dokumentation ist unfertig, auch wenn er läuft. Die Datei entsteht **im selben Commit** wie das
Feature, nicht später.

## Was in eine Feature-Datei gehört

Kurz, konkret, aus der Sicht von jemandem, der das Feature später ändern muss:

1. **Zweck** — welche fachliche Frage beantwortet das Feature? Ein bis zwei Sätze.
2. **Endpunkte** — Pfad, Parameter, Antwortform, Fehlerfälle.
3. **Datenquelle** — welche Tabellen, welche Joins, welcher Index. Bei jeder Abfrage: das
   `EXPLAIN`-Ergebnis und die gemessene Laufzeit gegen die Testkopie (Regel L7).
4. **Entscheidungen** — was wurde bewusst *nicht* gemacht und warum. Das ist der Teil, der
   sich sonst nicht rekonstruieren lässt.
5. **Regelbezug** — welche der unverhandelbaren Regeln aus Abschnitt 4 der Richtlinien betrifft
   das Feature, und wie ist sie umgesetzt.
6. **Offene Punkte** — was bekannt fehlt. Lieber notiert als vergessen.

### Die Nummernkreise — festgelegt am 21.08.2026

Es laufen zwei Nummernkreise nebeneinander, und bis heute sagte **keine** Datei, welcher gilt.
[`IMPLEMENTIERUNGSPLAN_MVP.md`](IMPLEMENTIERUNGSPLAN_MVP.md) führt das unter Schritt 9a als Befund:
*„weil dahinter zwei Nummernkreise stehen (projektweit in dieser Datei, je Feature-Datei bei 1
beginnend) und keine Datei sagt, welcher gilt."* Hiermit gesagt:

| Nummer | Kreis | Geprüft wird gegen |
|---|---|---|
| **E** — Entscheidung in einer Feature-Datei | **je Feature-Datei fortlaufend**, beginnend bei E1 | nur die eigene Datei |
| **M** — Messung | **projektweit fortlaufend** (seit 10.08.2026, `messungen-schritt6.md`) | alle Dateien, per `grep` vor der Vergabe |
| **Offener Punkt** | **projektweit fortlaufend** | alle Dateien |

**Dasselbe `E14` in zwei Feature-Dateien ist damit kein Fehler, sondern der Regelfall.**
[`benutzerverwaltung.md`](benutzerverwaltung.md) E14 benennt die Admin-Sperre,
[`prozess-katalog.md`](prozess-katalog.md) E14 die Spalte `traegt_nachrichten` — zwei Kreise, keine
Kollision. **Eine E-Nummer wird deshalb nie ohne ihre Datei genannt**, weder im Fließtext noch in
einem Auftrag: „E14" allein ist mehrdeutig, „`prozess-katalog.md` E14" ist es nicht.

> **Was das nicht heilt.** Der Plan verweist an mehreren Stellen mit **projektweiten** E-Nummern auf
> Entscheidungen, die in ihrer Feature-Datei anders heißen — `E20` meint dort
> [`benutzerverwaltung.md`](benutzerverwaltung.md) **E18**, `E23` meint
> [`prozess-katalog.md`](prozess-katalog.md) **E2**; dieselbe fehlgreifende Nummer steht laut Plan
> in fünf weiteren Dateien und ist nicht überall nachgezogen (u. a.
> [`rohdaten-frontend.md`](rohdaten-frontend.md)). **Diese Stellen sind mit dieser Festlegung falsch
> geworden, nicht richtig** — sie sind einzeln zu berichtigen, und das ist eine eigene Runde. Hier
> steht nur die Regel, nach der ab jetzt vergeben und gelesen wird.
>
> > **Und eine davon ist am 26.08.2026 vom toten zum irreführenden Verweis geworden.**
> > [`prozess-katalog.md`](prozess-katalog.md) führt seit diesem Tag ein **E23** — „ein Knopfdruck
> > erfasst alle Vorschläge des Mandanten" —, während der Plan mit derselben Nummer weiterhin die
> > entfallene Tabelle `partner` meint. **Die beiden Stellen im Plan tragen seither einen datierten
> > Korrekturkasten**; der alte Wortlaut bleibt daneben stehen. Der Rest des Rückstands aus diesem
> > Kasten ist unberührt und weiterhin eine eigene Runde.
>
> **Der Buchstabe `E` trägt außerdem eine zweite Bedeutung**, die von dieser Festlegung unberührt
> bleibt: In den Messdateien heißt `E<n>` eine *ergänzende Messung* und zählt **projektweit**
> (`messungen-schritt7.md`: „Die ergänzende Messung dieser Runde heißt **E6**, weil E1 bis E5 in
> `messungen-schritt6.md` vergeben sind"). Entscheidung und ergänzende Messung sind zwei Dinge mit
> demselben Buchstaben; auseinandergehalten werden sie durch die Art der Datei, in der sie stehen.

### Was nicht hineingehört — und was ausdrücklich doch

Geschützt sind **Nutzdaten und Namen Dritter**:

- echte Belegnummern und `MessagePropertyValue`-Inhalte
- **Partner**namen (`KE_OSTROV`, `DAS_DRAEXLMAIER` und dergleichen)
- Zugangsdaten, Passwörter, Passwort-Hashes, Zertifikate
- Hostnamen und Adressfragmente interner Server — auch dann, wenn sie in einem Dienstnamen stecken
  (`MULTI Service (<Knoten>) Produktion`)

**Nicht** geschützt ist das **Konfigurationsvokabular des Altsystems**: Bausteinmarken
(`NXS_FILE_CONVERT`, `FTPSender`), Prozess-, Projekt- und Ablaufnamen, Statuswerte, Mandantenkürzel.
Die zehn Mandanten stehen mit vollem Firmennamen in [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md)
§3.2; sie andernorts zu schwärzen, hilft niemandem und macht die Messdokumente unlesbar.

Enthält eine Marke oder ein Ablaufname den Namen eines **Partners** — nicht eines Mandanten —, wird
dieser Bestandteil maskiert (`<Kunde>`), wie in
[`messungen-schritt4.md`](messungen-schritt4.md) M13 geschehen.

> **Geschärft am 07.08.2026.** Hier stand nur „Zugangsdaten, Hostnamen, Produktionsdaten, echte
> Belegnummern, `MessagePropertyValue`-Inhalte. Beispiele werden anonymisiert." Das hat eine konkrete
> Frage nicht beantwortet: ob Bausteinmarken wie `NXS_FILE_CONVERT` als „Produktionsdaten" gelten,
> weil `NXS` ein Mandantenkürzel ist. Sie stand als offene Frage 12 in
> [`messungen-schritt5.md`](messungen-schritt5.md) und ist mit dieser Fassung entschieden: **Der
> Trennstrich läuft zwischen Nutzdaten und Konfiguration, nicht zwischen „intern" und „extern".**

## Offene Sichtprüfungen

*Angelegt am 11.08.2026.* **Diese Tabelle verweist und wiederholt nichts** — die Beschreibungen
bleiben, wo sie stehen. Sie existiert, weil solche Posten ab zwei Stück leise wachsen: Jeder einzelne
ist in seiner Feature-Datei gut begründet, und genau deshalb fällt niemandem auf, wie viele es
inzwischen sind.

| Schritt | Was zu sehen ist | Wo es beschrieben steht |
|---|---|---|
| 4 — Nachrichtenliste | Ob die vier Bedienelemente der Filterleiste unter 768 px sinnvoll umbrechen und ob die Statuszelle `Schritt: …` dort lesbar bleibt | [`nachrichtenliste.md`](nachrichtenliste.md), offener Punkt „Das schmale Fenster ist weiterhin ungesehen" (fortgeschrieben 11.08.2026) |
| 5 — Nachrichtendetail | Das Detailpanel am schmalen Fenster: Panel gegen eigene Route, §10.7 dort | [`nachrichtendetail.md`](nachrichtendetail.md) §10.10, Punkt 15 |
| 6, Teil 2b — Verkettung | Der Kettenblock am schmalen Fenster. Er liegt im Panel und erbt dessen Breitenverhalten; eigene Umbruchpunkte hat er nicht | [`verkettung.md`](verkettung.md) §8.11, Punkt 18 · offener Punkt in §11 |
| 7, Teil 3 — Belegsuche | **Drei getrennte Punkte, und der erste ist der wichtigste.** (1) **Das Suchfeld** in der Kopfzeile: Unter 768 px wird es eine eigene, volle Zeile — zu sehen ist, ob Belegart, Eingabe und `+` dort nebeneinander tragen und ob die Kopfzeile nicht auseinanderfällt. (2) **Die Marken** über der Trefferliste: ob acht davon umbrechen, ohne die Liste wegzuschieben, und ob die Schließen-Schaltflächen am Finger treffbar bleiben. (3) **Die Trefferliste**: Dort fällt unter `md` der *Ablauf* weg statt des Projekts wie in der Liste — zu sehen ist, ob Zeitpunkt, Status, Treffer und Kette in der Breite noch lesbar sind und ob **keine zweite Bildlaufleiste** entsteht | [`bam-suche.md`](bam-suche.md) §11.9 · §11.5 · offener Punkt 8 in §13 |
| 7, Teil 1 — Belegdaten | **Der BAM-Block am schmalen Fenster — seit dem 13.08.2026 der gewichtigste Posten dieser Tabelle.** Er bringt als einziger Block eine eigene Umschaltung mit (kein eigener *Umbruchpunkt* — es ist der des Projekts): Unter 768 px fällt er von zwei Spalten auf die gestapelte Form zurück, Beschriftung über den Werten. Zu sehen ist, ob die gestapelte Form trägt, ob die Marken lesbar bleiben und ob **keine zweite Bildlaufleiste** entsteht. **Ergänzt am 13.08.2026 (Nachbesserung):** Dort ist zusätzlich zu sehen, dass die **Endung** der Beschriftung auch in der gestapelten Form nicht aufbricht — sie trägt `white-space: nowrap` und gilt an beiden Einhängepunkten und unter jeder Breite. Der Deckel je Einhängepunkt (10/16 rem) betrifft diesen Posten dagegen **nicht**: Unter 768 px steht die Beschriftung ohnehin über den Werten, und keine der beiden Zahlen wirkt | [`bam-werte.md`](bam-werte.md) §15, Punkt 6 · §11a · offener Punkt in §13 |

| 7, Teil 4 — Präfixsuche | **Der Rückfall am schmalen Fenster**, drei Dinge und keine eigenen Umbruchpunkte — er erbt die des Leerzustands. (1) **Das Angebot** im Leerzustand: ob Frage, Knopf mit Beschriftung und Erwartungssatz untereinander tragen. (2) **Der Zeitraumhinweis** über der Liste — die Stelle, an der es eng wird: Er trägt **zwei Datumsangaben** und den Satz zu den nicht erfassten älteren Nachrichten, und beides darf nicht abgeschnitten werden. (3) **Der Präfixhinweis samt Rückweg-Knopf**: ob die beiden nebeneinander bleiben oder sinnvoll umbrechen | [`bam-suche.md`](bam-suche.md) §24 · offener Punkt 3 in §27 |

| 8 — Rohdaten und Protokolle | **Zwei Dinge, und das zweite ist neu in dieser Tabelle.** (1) **Die Zeitleiste mit ihren Zielen** bei **fünfzehn Artefakten** (M55) — sie erbt die Breite des Panels und hat keine eigenen Umbruchpunkte; zu sehen ist, ob Name, Dauer und Balken neben zwei Zeichen je Zeile noch tragen und ob die Zeichen am Finger groß genug sind. (2) **Die Dateiansicht am schmalen Fenster** — hier steht erstmals eine Festbreitenanzeige mit langen Zeilen; sie bricht **um** statt waagerecht zu schieben (Begründung: 36 von 206 Artefakten tragen kein Zeilenende, M61). Zu sehen ist, ob bei 360 px keine zweite Bildlaufleiste entsteht und ob eine große Datei als **ein** Textknoten flüssig bleibt. **Beides braucht eine Nachricht aus `2025-07-24` bis `2025-12-30`** — dem einzigen Fenster, in dem Datenbankkopie und Filestore-Kopie sich decken ([`START-LOKAL.md`](START-LOKAL.md) §1) | [`rohdaten-frontend.md`](rohdaten-frontend.md) §7 · §11, Punkte 1 und 9 |

| 9b — Katalogpflege | **Das schmale Fenster, und sonst nichts mehr.** Alle drei Routen sind am 24.08.2026 aufgerufen und ohne Konsolenfehler; nachgemessen in [`prozess-katalog-frontend.md`](prozess-katalog-frontend.md) §3: klebende Kopfzeile über 733 Zeilen (`main` und Kopfzeile beide bei y = 51, Dokument-`scrollHeight` = `clientHeight`, `window.scrollY` 0), aufklappendes Zeilenformular, Vorschlagsliste, zweistufiges `Escape`, Massenzuordnung bis zur Vorschau. **Zwei Befunde kamen dabei heraus** — eine Kopfzeile, die 16 px zu tief klebte, und eine Übersichtsseite, die gar nicht lud (§2 dort). Offen bleiben **drei Dinge bei 360 px**: ob die Tabelle mit ihren drei sichtbaren Spalten trägt, ob das Zeilenformular gestapelt lesbar bleibt, und ob die Vorschlagsliste des Partnerfeldes nicht aus dem Bild läuft. **Dazu zwei Wege, die bewusst nicht gedrückt wurden** — `PUT` und `AUSFUEHREN` schreiben Kuratierung auf die geteilte Testkopie, und eine kuratierte Zeile überlebt jeden Testlauf. **Nachtrag 26.08.2026 — der Dialog „Vorschläge übernehmen" kommt dazu.** Für ihn gilt dasselbe wie für `AUSFUEHREN`: Er ist **nur als Vorschau** anzusehen, denn Übernehmen schreibt Kuratierung. Dazu ein Befund, der die Sichtprüfung selbst betrifft: Auf `NEXANS` zeigt die Vorschau **nur den Nullzustand** („Es gibt keine unbestätigten Partnervorschläge"), weil dessen Katalog auf der Testkopie vollständig auf *gepflegt* steht (M93‑0) — **der Satz mit den drei Zahlen ist an `VOTG` zu sehen** (377 aus Regel A). Am schmalen Fenster fehlt zusätzlich, ob die vier Sätze bei 360 px lesbar umbrechen | [`prozess-katalog-frontend.md`](prozess-katalog-frontend.md) §12, Punkte 3, 4 und 9 bis 12 |
| 9a — Benutzerverwaltung | **In zwei Runden abgearbeitet, es bleibt eines offen.** Am 24.08.2026 durchgeklickt ohne einen Schreibvorgang; am 26.08.2026 mit dem eigens angelegten Wegwerfkonto `zz-wegwerf-9a` die Pfade nachgeholt, die ein verbrauchbares Konto brauchen. Belegt sind seither: **„nie angemeldet" und „Wechsel erforderlich"** an einem frischen Konto (beide Zustände gab es im Bestand nicht); **alle fünf Vorgänge, je genau ein Aufruf und danach kein `GET /api/admin/users`** — die Antwort setzt die Zeile; die **Mengenersetzung mit der vollständigen Zielmenge im Rumpf** und der Tausch aus E10; das Passwort in allen drei Lagen samt `passwort-unveraendert` am Abschnitt; **`409 selbstschutz` am eigenen Konto ohne Dialog, mit zurückspringendem Schalter** — die Entscheidung „auffangen statt vorwegnehmen" am laufenden System; **`lockedUntil` gefüllt** nach fünf absichtlichen Fehlversuchen, „nicht gesperrt" neben „bis 26.08.2026, 09:44" — genau der Fall, für den E20 gebaut wurde; und die **klebende Kopfzeile über 44 Zeilen** (y = 51, kein Dokumentscroll). **Ein Befund kam dabei heraus und ist behoben:** Nach jeder Mengenersetzung ging ein zusätzliches `GET /api/mandanten` hinaus. **Offen bleiben zwei Dinge:** (1) **360 px** — die Browsersteuerung kann das Fenster nicht verkleinern. (2) Der Zustand **„kein Zugriff"** — er braucht eine Anmeldung als `MANDANT`, und Passwörter tippt Claude Code nicht; das Wegwerfkonto steht dafür bereit | [`benutzerverwaltung-frontend.md`](benutzerverwaltung-frontend.md) §14 |

**Bei acht der neun ist der Grund derselbe:** Die Browsersteuerung kann das Fenster nicht
verkleinern; geprüft ist jeweils das Regelwerk, nicht die Darstellung. Sie sind **von Hand
nachzuholen** und verschwinden aus dieser Tabelle, sobald das geschehen ist.

**Die neunte — 9a — ist die einzige, bei der das schmale Fenster nicht der einzige Rest ist**, und
sie ist am 26.08.2026 bis auf zwei Punkte abgearbeitet worden. Der Weg dahin führte über ein eigens
angelegtes Wegwerfkonto: Jeder der fünf Vorgänge schreibt auf die geteilte Testkopie und wirft ein
Konto aus allen Sitzungen, das lässt sich an einem echten Konto nicht nebenbei prüfen. **Das Konto
bleibt stehen** — es gibt kein Löschen (E8) — und heißt `zz-wegwerf-9a`. Es ist für die letzte
offene Prüfung noch **aktiv**; sobald „kein Zugriff" gesehen ist, gehört es deaktiviert.

> **Was hier nicht hingehört:** was von Hand **grundsätzlich** nicht zu sehen ist. `tiefeErreicht`
> und `zyklusErkannt` aus [`verkettung.md`](verkettung.md) §8.5 sind seit dem 11.08.2026 durch
> Render-Tests belegt ([`frontend-grundlagen.md`](frontend-grundlagen.md) §9) — eine Unmöglichkeit
> ist keine offene Sichtprüfung, sondern eine Testaufgabe.

## Verzeichnis

### Grundlagen — immer gültig

| Datei | Inhalt |
|---|---|
| [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) | **Die verbindliche Wahrheit.** Zweck, Rollen, Quellsystem, fachliche Definitionen, Architektur, Sicherheit, Leistungsregeln, MVP-Umfang, Annahmen |
| [`IMPLEMENTIERUNGSPLAN_MVP.md`](IMPLEMENTIERUNGSPLAN_MVP.md) | Die zehn Schritte bis zum MVP, mit Abgrenzung und Abnahmekriterium je Schritt |
| [`datenmodell.md`](datenmodell.md) | **Nachschlagewerk für jede Abfrage.** Kerntabellen, Mandantenkette, Indizes, dokumentierte Fallstricke |
| [`START-LOKAL.md`](START-LOKAL.md) | **Was lokal anders ist als produktiv** — damit niemand einem Fehler nachjagt, den es nicht gibt. Angelegt in Schritt 8 wegen des tragendsten Falls: Von der Entwicklungsmaschine sind **63,2 % des Bestands nicht abrufbar**, weil zwei Dateiablagen abgeschaltet sind; abrufbar ist allein `2025-07-24` bis `2025-12-30`. Dazu die langsame Testkopie, die fünf leeren Monate und was lokal gar nicht angesprochen wird. **Keine Feature-Datei** — sie beschreibt die Umgebung und nicht, was gebaut wird, und steht deshalb hier |

### Features

Angelegt:

| Datei | Aus Schritt | Inhalt |
|---|---|---|
| [`datenzugriff.md`](datenzugriff.md) | Schritt 2 | Zwei DataSources, zwei DSLContexts, ein Transaktionsmanager, dreischichtiger Schreibschutz, Flyway, Zeitquellen, Fehlerformat, jOOQ-Codegenerierung, Tests |
| [`message-status.md`](message-status.md) | Schritt 2 | Statuskatalog, `MessageStatusClassifier`, die eine Fehlerbedingung (vorgezogen aus Schritt 3). **Seit Schritt 6, Teil 2a:** `ZWISCHENSCHRITT` ist in **`AUFGETEILT`** (`SPLITTED`) und **`ZUSAMMENGEFUEHRT`** (`MERGED`) zerfallen — technisch dasselbe, fachlich Gegenteiliges; `istEndstatus` liefert für beide unverändert `true`, die Überfälligkeitsrechnung bleibt unberührt. Dazu der Abstrich, der dazugehört: Über die Kette hinweg ist der **Status unzuverlässig** — bei `IBIS`, `IBISGUS` und `ZAST` trägt die Wurzel `FINISHED` (M24‑3), verlässlich ist `Source` und nicht `MessageStatus` |
| [`annahmen-korrekturen.md`](annahmen-korrekturen.md) | Schritt 2 | Was die Erhebungen gegenüber der Projektbeschreibung verändert/bestätigt haben — **und seit dem 14.08.2026 auch, wo dieses Projekt selbst geirrt hat**: der Kommadefekt der BAM-Suche, der seit Teil 2b in einem als fertig gemeldeten Pfad steckte und erst beim Bau eines Isolationstests auffiel |
| [`authentifizierung.md`](authentifizierung.md) | Schritt 3, Teil 1 | Anmeldung, Sperre und Auskunftsdisziplin, Sitzung und Cookie je Profil, Passwortwechsel, Bootstrap, Nutzer anlegen — und warum die Altnutzer bewusst **nicht** übernommen werden |
| [`mandantentrennung.md`](mandantentrennung.md) | Schritt 3, Teil 1, ergänzt in 9a und 10a | `MandantContext`, Berechtigung als Menge statt Rolle, die **drei** benannten Endpunkt-Ausnahmen von Regel M1 (§3), die **zwei** benannten Ausnahmen von Regel M2 (§4: `@OhneMandantenkontext` und seit dem 26.08.2026 `RollupLeseRepository`), die ArchUnit-Regeln, die Vorlage für den Isolationstest |
| [`visuelles-konzept.md`](visuelles-konzept.md) | Schritt 3, Teil 2 | Farbrollen samt fachlicher Bindung der Statusfarben, die vier Stufen des Akzents `#b9c022` mit gemessenen Kontrastwerten, „Status nie allein über Farbe", Typografie, Dichte nach Zeigergerät, volle Fensterbreite und die eine Ausnahme davon, Verhalten am Handy — und wie man das Konzept in einer Datei ändert |
| [`frontend-grundlagen.md`](frontend-grundlagen.md) | Schritt 3, Teil 2 | Rewrite statt CORS, warum die Routensperre kein Schutz ist, Ablauf nach dem Anmelden, Aufbau der Sprachdateien, Regeln für den Zwischenspeicher, Zuordnung von `type` zu Übersetzung, Scroll-Architektur des Rahmens |
| [`nachrichtenliste.md`](nachrichtenliste.md) | Schritt 4, überarbeitet in Schritt 6 Teil 2a, erweitert in **Schritt 10b‑1 Teil A und B** | Listen-Endpunkt mit Pflicht-Zeitfenster, Cursor-Paginierung in der gemessenen ODER-Form, Filter über `MessageStatusKind`, Freitext gegen die Stammdaten samt der gemessenen Fenstergrenze von 30 Tagen (aufhebbar bis 90), die Mandantenkette als `EXISTS` statt über die View — und warum. **§5 ist seit dem 11.08.2026 die Geschichte des entfallenen Ausblende-Schalters:** was gebaut war (`zwischenschritte=false`, Chip, `GET /api/nachrichten/merkmale`), was die Messung ergab (96,9 % der ausgeblendeten Wurzeln tragen BAM-Werte gegen 2,4 % der gezeigten Kinder, M26) und warum er **ersatzlos** fällt statt gedreht zu werden — ein Stellungsprädikat verstecke bei `IBISGUS` **100 %** und bei `ZAST` 92,93 % aller Zeilen (M28‑1), und eine Vorgabe, die je nach Mandant zwischen 0 % und 100 % versteckt, ist keine. Dazu der Vermerk zur **L15-Falle**, die mit dem Merkmale-Endpunkt verschwindet (13,2 s gegen 11,9 ms) — *nicht weil sie langsam war, sondern weil die Frage nicht mehr gestellt wird*. **§8.1: Die Statuszelle nennt keine Präposition mehr** — „wartet vor" ist durch M29 widerlegt, „in" gegen „vor" bräuchte einen Join auf `MessageAction` je Zeile, und der Weg über `offenerZustand` im Listen-Endpunkt ist erwogen und **verworfen**. §8.2 die URL ohne den Parameter (ein alter Link wird übergangen, nicht abgewiesen), §8.3 der Leerzustand ohne die eine Klausel. **§5a (27.08.2026) ist die Untersuchung des Listenplans — und sie endet ohne Bau:** Sechs Fassungen sind gegen offenen Punkt 57 gemessen, zwei bringen `SUTTONS` von 1.034,784 ms auf rund 7 ms, **und jede von ihnen zerstört etwas anderes** — die eine den Prozessfilter (Faktor 894), die andere drei der acht bis dahin ungemessenen Mandanten (`NXHBE` Faktor 3.002). Der Grund steht darunter: Die Liste hat **zwei Planfamilien**, und jede ist für eine andere Klasse von Mandanten falsch — zeit-getrieben liest bis 51 Treffer beisammen sind (schlecht für dünne Mandanten: `EDITIONLINGERI` **2,3 s** für null Zeilen), prozess-getrieben liest den **ganzen Lebensbestand** des Mandanten (schlecht für volumige). Beide Wahlen ruhen auf derselben Schätzung von 197.804 Zeilen, weil drei Indizes die `CARDINALITY` 18 tragen (M83). **Der Index, der beides löste, existiert nicht und dürfte nicht entstehen** — er läge auf `GlassfishDB`. Neu gefunden und in keinem offenen Punkt: `EDITIONLINGERI` mit 2,3 s, `ZAST` mit 289 ms und der **Prozessfilter bei `NEXANS` mit 7.459,912 ms**, also 74,6 % der Zeitgrenze des Lese-Pools (offene Punkte 63 bis 65). **§5b ist der gebaute Parameter `ueberfaellig`** (E‑j): das SQL-Gegenstück zu `MessageStatusClassifier.istUeberfaellig`, mit aus `istEndstatus` **gezogener** statt getippter Statusmenge, und mit dem Satz an vier Stellen, den offener Punkt 56 verlangt — **er ist kein Filter, sondern eine zweite Abfrageform**: `MessageStatusIDX` statt `MessageLastUpdateIDX`, `filesort` statt Indexsortierung, und der Cursor bleibt bei `key_len = 123`. Er **rettet** `SUTTONS` um Faktor 159 und kostet `NEXANS` Faktor 2,9. Ein **Plantest** hält das fest (nicht weil es gut ist, sondern damit eine Änderung auffällt), ein Blättertest über **elf Seiten** weist nach, dass der entwertete Cursor trotzdem 538 Zeilen liefert — jede genau einmal —, und drei Isolationstests nutzen aus, dass **alle** überfälligen Zeilen des Bestands `NEXANS` gehören: Fiele der Mandantenfilter, sähe `SUTTONS` nicht ein paar fremde Zeilen, sondern genau diese 538 |
| [`prozessauswahl.md`](prozessauswahl.md) | Schritt 4 | `GET /api/prozesse` — die Liste, aus der der Prozessfilter wählt. Stammdaten ohne Zeitfenster und ohne Cursor, die Mandantenkette hier als Join statt als `EXISTS`, und warum die Gegenprobe des Isolationstests bei einem Endpunkt ohne Parameter anders aussieht |
| [`nachrichtendetail.md`](nachrichtendetail.md) | Schritt 5 (Teil 1 §1–§9, Teil 2 §10), ergänzt in Schritt 6 Teil 2b | Die beiden Detail-Endpunkte: Kopf, Schrittfolge und kuratierte Eigenschaften in einem Aufruf, alle technischen Eigenschaften auf Abruf. Die **dreistufige Namensauflösung** (`DIREKT` → `HERGELEITET` → `ROHWERT`) samt der Eindeutigkeitsbedingung, die sie vor dem Raten bewahrt — sie hebt den Anteil benannter Schritte von 71,5 auf **99,7 Prozent**. Der **offene Zustand** in **sechs** Werten (`LAEUFT_AUF`/`WARTET_IN`/`WARTET_VOR`/`EMPFANGEN`/`OHNE_AKTION`/`KEINER`) — `OHNE_SCHRITT` trug bis zum 10.08.2026 zwei Fälle und ist in eine Auskunft über die **Plattform** (`EMPFANGEN`, angekommen und seitdem nicht weitergelaufen, mit Wartedauer ab dem Metadaten-Schritt) und eine über die **Datenlage** (`OHNE_AKTION`, kein Ablauf protokolliert, ohne Anker und deshalb ohne Wartedauer) aufgeteilt worden, die kuratierte Auswahl mit Befüllungsquote je Name **und je Mandant**, die harte Byte-Kappung der Werte, `EXPLAIN` und Laufzeit aller sechs Statements gegen drei Bezugsgestalten — und die bewussten Nicht-Entscheidungen (keine Zuordnungstabelle, kein Gerüst, keine Obergrenze) mit den Zahlen dahinter. **§3a (10.08.2026):** Wartedauer, Frist und **Überfälligkeit** — Problemkategorie 2 aus §4.2, gerechnet gegen die **Anwendungsuhr** und an genau einer Stelle (`MessageStatusClassifier.istUeberfaellig`) —, dazu die Gesamtdauer und die beiden **verschiedenen Mengen rund um den Metadaten-Schritt**, jeweils begründet: eingeschlossen beim fachlichen Start, ausgeschlossen beim „letzten Schritt". **§10 ist die Oberfläche:** zwei Einhängepunkte (`?nachricht=` und `/nachrichten/<id>`) mit **einer** Komponente und ohne abfangende Routen, die auf **die Nachricht normierte** Zeitleiste, die Wartezeile *wartet seit … · Frist …* am offenen Zustand, die Herkunft des Schrittnamens im Tooltip, die Eigenschaften erst beim Aufklappen — und der für „gibt es nicht" und „gehört einem anderen Mandanten" **identische** Fehlertext, der stattdessen den Mandanten in der Kopfzeile nennt. **§10.12 führt jeden ungesehenen Punkt mit seinem Grund** — Versäumnis oder Unmöglichkeit —, darunter die **gebaute und wieder entfernte Lückenzeile** samt der gemessenen Sekunde, die sie erledigt hat. **Ergänzt am 11.08.2026:** `rollen` im Kopf — immer vorhanden, leer statt fehlend, und **ohne einen einzigen zusätzlichen Zugriff**, weil die vier Verkettungsspalten auf der Zeile stehen, die `findeKopf` ohnehin liest (E4); dazu der Kettenblock zwischen Kopf und Zeitleiste (§10.4a, vollständig in [`verkettung.md`](verkettung.md) §8) |
| [`verkettung.md`](verkettung.md) | Schritt 6 (Teil 1 §1–§7, Teil 2b §8) | Die beiden Ketten-Endpunkte: `…/kette` und der cursor-basierte `…/kette/abwaerts`. **Vier Spalten, zwei Beziehungen** — `SourceMessageID`/`Source` tragen die Aufteilung, `TargetMessageID`/`Target` die Zusammenführung; eine Spalte allein verlöre die Hälfte der Fälle (M25‑2). Die **bewusst unsymmetrische** Auflösung: nach oben vollständig (je Ebene ein `const`-Zugriff, 0,5 ms, nie mehr als ein Glied darüber), nach unten **genau eine Ebene** (bis **3.350** Kinder und **897** Merge-Eingänge, M30‑2). Die **drei Grenzen** — Tiefe 10, Breite 50, Zyklus — mit dem Befund, dass zwei davon in der Testkopie **nie** ansprechen und die dritte **ständig**: Die Breitengrenze greift bei 11,38 % der Merge-Ergebnisse, Tiefengrenze und Zyklusschutz kein einziges Mal — und bleiben trotzdem, weil die Kette durch Datenbank-Events entsteht, die uns nicht gehören. Die **Rolle als Menge** (`common/Kettenrolle`, `common/Kettenrollen`) samt dem **Ersatz für das vollständige `switch`**, den es bei einer Menge nicht geben kann — ein Bestandstest gegen die Testkopie mit **allen sechs Paaren** in der hinterlegten Liste, auch den **drei** gemessen leeren (alle drei enthalten `MERGE_EINGANG`; nur eines davon ist durch M28‑1 auch wirklich ausgeschlossen). Dazu: warum die Flags `Source`/`Target` in der *Liste* die richtige Abkürzung sind und **hier nicht**, warum die **Zählung** denselben Mandantenfilter trägt wie die Zeilen (und was das kostet: 18,7 ms statt `Using index`), und warum die `bit(1)`-Falle aus `datenmodell.md` §5.8 im Java-Code **nicht** greift. **§8 ist die Oberfläche (Teil 2b):** der Kettenblock zwischen Kopf und Zeitleiste, der **gar nicht erscheint**, wenn `rollen` leer ist — und dann auch keine zweite Anfrage stellt; genau deshalb darf er offen stehen statt eingeklappt. Dazu die Einteilung **nach der Flussrichtung statt nach der Richtung der API** — beim Merge liegt das Ergebnis im `aufwaerts` und die Eingänge im `abwaerts`, und wer die beiden Listen unverändert beschriftete, schriebe bei **38.628** Zeilen das Gegenteil dessen hin, was passiert ist —, die Zahl in der Überschrift **nur dort, wo der Endpunkt eine liefert** (M30), Navigation über den bestehenden Parameter `nachricht`, **Nachladen im Block statt eines Sprungs in die Liste** samt der L1-Begründung, und warum der Block **keine eigene Farbe** trägt. **Nachgearbeitet am 11.08.2026 (§2):** Die beiden Listen heißen `aufwaerts`/`abwaerts` statt `vorgaenger`/`nachfolger` — Namen nach dem **Mechanismus**, weil die alten nur bei der Aufteilung stimmten und die Aufgabenstellung zu Teil 2b genau daran gescheitert ist; dazu `abwaertsCursor`, mit dem der erste Klick auf „Mehr laden" **eine** Anfrage kostet statt zwei. **Fortgeschrieben am selben Tag:** §8.3 nennt den zickzackenden Aufstieg nicht länger „richtig so", sondern eine **bekannte Verzerrung** — die Zeile sagt die Wahrheit, die Überschrift nicht —, beziffert sie mit den **390 Ansichten** aus M31‑1 gegen die 38.628, die die Einteilung begründet haben (Faktor 99), und stellt **beide** Handlungsmöglichkeiten dar, **ohne eine zu wählen**; die Entscheidung fällt im Sparring zu Schritt 7, das Verhalten ist unverändert. **§8.14** ist das Netz unter der Konsole: `console.error` lässt den Frontend-Testlauf fehlschlagen, und drei — genau drei — gerenderte Bäume decken ab, was von Hand nicht zu sehen ist |
| [`bam-werte.md`](bam-werte.md) | Schritt 7, Teil 1 (Backend §1–§10, Oberfläche §11 und §11a) | Die Belegdaten am Nachrichtendetail: `GET /api/nachrichten/{id}/bam` und das Feld `bamAnzahl` im Kopf. **Die Zweiteilung ist der Kern** — eine Zählung je Typ liefert die *wahre* Zahl, eine zweite, im Statement über `ROW_NUMBER() OVER (PARTITION BY …)` gedeckelte Abfrage die höchstens **20** angezeigten Werte je Gruppe. Ohne sie hinge die Antwortgröße an einer **ungemessenen** Zahl: M41 misst 9.296 Werte auf *einer* Nachricht, über den ganzen Bestand ist das Maximum unbekannt. Der Nachweis steht als Messung daneben — dieselben **20 Zeilen** bei 9.296 wie bei 445 Werten, während der ganze Aufruf im teuersten bekannten Fall **31,6 ms** kostet. **Die Konfiguration aus `MessageBAMMandant` ordnet und siebt nicht** (M40): `WOC` hat null konfigurierte Typen und trägt trotzdem 2.067 BAM-Zeilen — folgte der Block ihr, sähe dieser Mandant *nichts*; der zweite und schwerere Grund kommt aus Teil 3, wo die Suche typlos ist. Dazu der Befund der Messung selbst: **Die erste Fassung der Zählung war siebzehnmal teurer, bei identisch gutem `EXPLAIN`** — die Stammdaten-Joins liefen je Zeile statt je Gruppe (68,9 gegen 3,8 ms), und am typischen Fall ist der Unterschied null; ein Test hält die Gestalt seitdem fest. **§11 ist die Oberfläche:** der Block **zwischen Kettenblock und Zeitleiste** — *welcher Beleg ist das* vor *was ist damit passiert* —, eingeklappt mit der Zahl aus dem Kopf, und bei `bamAnzahl === 0` **gar nicht vorhanden**, ohne Rahmen und ohne Anfrage (80,6 % der Nachrichten, bei Merge-Eingängen **alle**). Der Schlüssel der Liste ist `(typ, wert)` und niemals der Wert allein: Bei 4,17 % der Paare steht derselbe Wert unter mehreren Typen (M37), und doppelte React-Schlüssel sind für Vitest unsichtbar. Ehrliche Restangabe statt „mehr laden", **keine Normalisierung** (führende Nullen bleiben stehen — das ist ein Thema der Suche), **kein Verweis in die Suche** (er hätte kein Ziel). Die offenen Punkte benennen jeweils die Messung, die sie entscheiden würde — insbesondere für die Endungen `_K_SAP`/`_L_SAP`/`_FORS`, die nach Regel Q4 stehen bleiben. **Nacharbeit vom 13.08.2026 (§11a und §15):** Der Block zeigt dieselben Werte auf rund einem Fünftel der Höhe — Werte **nebeneinander als Marken**, Beschriftung links in einer **gedeckelten** Spalte (`--dichte-beschriftung`, 10 rem), unter 768 px gestapelt; am Endpunkt ändert sich **nichts**. Der tragende Satz ist gemessen und nicht behauptet: **Die Marke ist die Wortgrenze**, denn **2,97 %** der BAM-Werte tragen ein Leerzeichen *innen* (E7, 27.792 von 936.529 über Fenster B, angeführt von 9018 — dem Typ, den `NEXANS` auf 92,26 % seiner Wurzeln trägt); ein Trennzeichen schiede damit aus, und für jedes andere Zeichen wäre es geraten. Der Deckel von 10 rem folgt aus der Regel *in einer Zelle weicht die Hauptinformation nicht*: Im Panel bleiben so 282 px für die Werte, und die längste gemessene Belegnummer braucht 285 px — **eine breitere Beschriftungsspalte zwänge die Belegnummer in den Umbruch**. Und der erste offene Punkt ist entschieden: **M45** misst, dass die Kürzung der Typbeschreibungen **nicht** verlustfrei wäre (62 Beschreibungen werden 57), wobei zwei der drei Kollisionen die *endungslosen* Grundtypen 0 und 3 treffen und `Abladestelle_L_SAP`/`Abladestelle_K_SAP` auf **3.405 Nachrichten eines Monats gemeinsam** stehen |
| [`bam-sollaengen.md`](bam-sollaengen.md) | Schritt 7, Teil 2a | Die Tabelle `overlord_monitor.bam_sollaenge` (`V5__bam_sollaenge.sql`): auf welche Länge die Suche einen Begriff mit Nullen auffüllt, und bei welchen Typen sie zusätzlich eine Fassung mit **führendem** Leerzeichen sucht. **Reine Kuratierung, kein Endpunkt** — der entsteht in Teil 2b. Der tragende Befund ist der Schlüssel: Die Sollänge ist **keine Eigenschaft des Typs**, sondern eine des Paares aus Mandant und Typ, und das ist an **drei unabhängigen Stellen** gemessen (M46‑2) — Typ 2000 dominiert bei `SUTTONS` mit Länge 6 und bei `VOTG` mit Länge 7; Typ 9014 fällt über den Bestand mit 58,45 % durch, erreicht bei **`WOC`** aber **95,21 %** und ist dort der nützlichste Eintrag überhaupt; und Typ 2001 hat bei `VOTG` zwar 100 % Dominanz, aber **null** Werte mit führender Null. Ein typweiter Schlüssel hätte alle drei falsch entschieden. Die Regel ist mechanisch (mindestens ein Wert mit führender Null **und** mindestens 95 % Längendominanz über den **Bestand**) und ergibt **14 kuratierte Paare aus 45 gemessenen**, dazu zwei Zeilen für das führende Leerzeichen — 9018 mit 31.193 von 2.311.236 Zeilen und 9020 mit **einer einzigen**; einen leeren BAM-Wert gibt es im ganzen Bestand nicht. Die Datei führt jede Zeile mit ihrer Dominanz, ihrem `n` und der Entscheidung daneben — **einschließlich der drei Stellen, an denen die Regel sichtbar an ihre Grenze stößt** und die deshalb nicht stillschweigend geglättet werden: Zwei der vierzehn Einträge (`IBIS`/1, `SUTTONS`/2000) können **nachweislich nichts finden**, weil bei ihnen kein Wert *mit* führender Null auf der Sollänge liegt; und die seit Schritt 4 kuratierte Lieferschein-Nr. **9006 fällt mit 94,21 % durch** — 0,79 Prozentpunkte unter der Schwelle, ausgerechnet der einzige Typ, für den M43‑4 die Wirkung des Auffüllens an echten Werten belegt hat (roh **1.642** Treffer, aufgefüllt **4**). Die Schwelle zu senken hilft dort nicht: Bei 94 % kämen drei Typen mit 0,21 bis 5,71 % führender Null mit. Gesichert ist die Tabelle durch `BamSollaengeDriftDbIT` — eine Abfrage **je Paar**, weil die Sammelform mit 6,8 s am `max_statement_time=10` des Lese-Pools stirbt —, dessen Gegenprobe in beiden Zweigen gelaufen ist; **der Vorbehalt steht dabei und nicht in einer Fußnote:** Er läuft **nicht in der CI**, dieselbe Garantiestufe wie beim Statustest, und er prüft, was dasteht, **nicht was fehlt**. §7.2 legt außerdem die **Sicherungsregel für handkuratierte, nicht wiederherstellbare Daten** an — sie existierte bis dahin nicht, was hier als Befund vermerkt ist, und führt von Anfang an auch `process_catalog` *(Stand 20.08.2026 — `partner` entfällt, E23)* |

| [`bam-suche.md`](bam-suche.md) | Schritt 7, Teil 2b (Backend §1–§9), **Teil 3** (Oberfläche §10–§14), **Teil 4** (Präfixsuche im Backend §15–§21, **Oberfläche §23–§27**) und der **Nachtrag §22** (das Komma) | **Teil 4, die Oberfläche (14.08.2026) — der Rückfall wird angeboten und läuft erst auf Zutun.** Findet die Suche nichts, steht **im Leerzustand der Trefferliste** die Frage, ob nach Nummern gesucht werden soll, die *damit anfangen* — an genau einer Stelle, ohne Fachwort und mit einem Satz Erwartung, weil das Ergebnis anders aussehen wird. **Vier Bedingungen, und die vierte ist die, die durchrutscht:** Die Antwort meldet `EXAKT`, die Liste ist leer, es gibt einen Begriff — und die Suche wurde **nicht abgebrochen**; wer gerade an der Zeitgrenze gescheitert ist, bekommt keine teurere Suche angeboten. Sie stehen als **reine Funktion** und nicht als Zweigreihenfolge im Markup, denn die wäre bei der nächsten Umstellung still weg. **Der Knopf verkleinert das Zeitfenster selbst**, wenn es über dem 30‑Tage-Deckel liegt: `bis` bleibt, `von` rückt nach, das neue Fenster ist ein **Ausschnitt** des alten. Dass das nicht zwei Dinge auf einmal ändert, ist der Kern der Entscheidung — die exakte Suche über das große Fenster war **leer**, über einen Ausschnitt daraus ist sie es zwangsläufig auch. **Der Preis ist die Sichtbarkeit:** Über der Liste stehen **beide Datumsangaben** und der Satz, dass ältere Nachrichten nicht erfasst sind, sonst liest der Nutzer „nicht gefunden" als „nicht vorhanden". **Beim Rückweg auf „genau suchen" bleibt das verkleinerte Fenster stehen** — ein bewusst in Kauf genommener Nachteil, der als solcher geführt wird: Ein Wert, der sich beim Moduswechsel von selbst änderte, wäre versteckter Zustand, und der Zustand steht hier vollständig in der URL. **`modus` steht dort neben `begriff`, `von` und `bis`**, ohne `withDefault` — nur der Präfixmodus erscheint, und `null` zu setzen *ist* der Rückweg; **jede Änderung an den Begriffen fällt auf `exakt` zurück**, das Fenster bleibt. Die **Abschneidemeldung** rät im Präfixmodus zuerst zu **mehr Zeichen** und erst danach zum Zeitraum, und der Rat stimmt: Die Kostengröße ist die **Trefferzahl** (E6). Die **Belegart** wird an genau einer Stelle erwähnt — in der Hilfe im Leerzustand, und nur, wenn der Mandant überhaupt welche hat; **kein Sonderfall für 9006**, das bleibt eine Entscheidung des Auftraggebers. **Kein neuer Marken-Baustein, kein neuer Umbruchpunkt, kein neuer gerenderter Testbaum.** Die Sichtprüfung hat zwei Dinge zusätzlich gezeigt: **„Auf ein Jahr erweitern" stand im Präfixmodus noch da** — ein Klick in ein garantiertes `400`, und damit ein Bruch der Zusage, dass die Oberfläche diesen Fehler nie selbst auslöst; behoben. Und: **Ein Fehler, der wiederholt wird, erscheint in der Browsersteuerung nie** (`fetchStatus: paused`) — gegengeprüft an einem `404`, der sofort erscheint, und am **unveränderten** Pfad der Nachrichtenliste, der genauso hängt; eine Eigenschaft der Umgebung und nicht des Codes. `praefixsuche-fenster-zu-gross` wird trotzdem **nicht mehr wiederholt**, aus eigenem Grund: Der Fehler steht an der Parameterform fest. **Was offen bleibt und hingeschrieben gehört: Der Nutzen dieser Funktion ist unbeziffert.** Wie oft die exakte Suche im Betrieb leer ausgeht, stünde nur in einem Suchprotokoll, und es ist keines gebaut worden — **die Kosten sind gemessen, der Nutzen ist es nicht**. Ebenso offen: der Abbruchfall (am laufenden System nicht herbeizuführen — der schlimmste exakte Wert über ein Jahr *und* ein einzeichiger Präfix laufen beide durch) und das schmale Fenster. **Nachtrag §22 (14.08.2026) — der Kommadefekt, und er ist kein offener Punkt, sondern ein Irrtum.** Ein Wert mit Komma war seit Teil 2b **unsuchbar**: Spring zerlegt einen einzeln gesetzten `@RequestParam` am Komma, der zweite Teil trug keinen Pflichttrenner mehr, die Antwort war `400`. **In beiden Modi**, weil der Fehler vor dem Modus sitzt. Gefunden beim Bau des Isolationstests zu Teil 4 — alle 25 längsten `NEXANS`-Werte tragen eines. **M51** beziffert die Tragweite: 55.989 Werte, aber **drei Typen von 62**, davon **96,62 % unter Typ 9003** allein, und alle drei bei **einem** Mandanten. Behoben über einen controller-eigenen `@InitBinder` — **nur an diesem Endpunkt**; die Nachrichtenliste trennt `status` und `prozess` weiterhin am Komma, wo es gemessen folgenlos ist (0 von 1.503 `ProcessID`). Am Statement ändert sich **nichts**, das Komma ist kein Platzhalter. Der Vorgang selbst steht in [`annahmen-korrekturen.md`](annahmen-korrekturen.md). **Teil 4 (14.08.2026) — die Präfixsuche, und die Entscheidung dahinter.** Der Endpunkt bekommt **einen** Parameter: `modus=exakt|praefix`, Vorgabe `exakt`. **Ohne ihn verhält sich alles Zeichen für Zeichen wie vorher** — dieselben Statements, dieselben Varianten; ein Test vergleicht die beiden Rümpfe. **Die Präfixsuche kommt also, aber nicht als Voreinstellung, und der Grund ist nicht der Preis**, sondern M49‑3: **Ein vollständig eingetippter Wert findet als Präfix 23 Nachrichten statt einer** — als Voreinstellung änderte sie die Antwort auch für den Nutzer, der nichts falsch macht. Im Nulltreffer-Fall gibt es diese Kehrseite nicht. Im Statement ändert sich **ein** Ausdruck: aus `MessageBAMValue IN (…)` wird `(MessageBAMValue LIKE ? ESCAPE '\' OR …)`; **das Maskieren ist Pflicht** (M49‑4: `_` steht in 2.696 Werten — dieselbe Falle wie Regel Q1) und an der gebauten Fassung belegt, nicht behauptet. **Auffüllen und Präfix ankern gegeneinander** (M49‑1, in allen acht Fällen), deshalb wandern die Nullen **ins Muster** — aber **nur für Begriffe mit Typ und Sollänge**; ein Begriff ohne Typ bekommt genau **eine** Fassung, sonst wären es bei acht Begriffen bis zu 56 `LIKE`-Zweige. **Die Typwahl bekommt damit eine zweite Rolle**, ohne M36 zu widersprechen: Dort geht es um Geschwindigkeit, hier darum, welche Fassungen bildbar sind. Gemessen ist der Bau vorher in **M50** — dem Faktor‑8,5-Fall, den M49 ausdrücklich offengelassen hatte: Der schlimmste Präfix des **Bestands** (1.332.180 Zeilen, hergeleitet und nicht abgeschrieben) kostet über 30 Tage **3,851 s** und ist über ein Jahr **an der 60‑Sekunden-Grenze abgebrochen**. Nach der vorregistrierten Lesart heißt das: **Der Präfixmodus ist auf 30 Tage gedeckelt** (`400 praefixsuche-fenster-zu-gross`, mit beiden Zahlen im Rumpf), die exakte Suche behält ihr Jahresmaximum. **Eine Mindestlänge bleibt aus** — jetzt aus einem *neuen* Grund: Die schlimmste Trefferzahl hat einen **Boden** bei 234.159, den keine Präfixlänge unterschreitet (M49‑2a mit M33). Der Befund, der in keiner Zeile des Auftrags stand: **Der Plan kippt.** Bei 1,33 Millionen Kandidatenzeilen steigt der Optimierer über `MessageLastUpdateIDX` ein und probt `MessageBAM` über den Primärschlüssel — `MessageBAM_BAMValueOnly` steht nur noch unter `possible_keys`. **Das ist die richtige Wahl und das schärfste Argument gegen ein `STRAIGHT_JOIN`, das dieses Projekt hat.** Und ein zweiter, gefunden beim Bau statt beim Messen: **55.989 Werte (0,363 %) tragen ein Komma**, Spring zerlegt den wiederholbaren Parameter daran — **sie sind heute unsuchbar, in beiden Modi und seit Teil 2b**. Nicht behoben, weil die Änderung den exakten Pfad träfe; festgeschrieben in einem Test und als offener Punkt geführt. **Die Oberfläche ist nicht angefasst** — der Rückfall ist am Endpunkt vorhanden und noch nicht bedienbar. **Teil 3 (13.08.2026) — die Oberfläche, und ein zweiter kleiner Endpunkt.** `GET /api/bam/typen` liefert die in `MessageBAMMandant` konfigurierten Belegarten als **Auswahl** neben dem Suchfeld — **keine neue Kuratierungstabelle**: Die Kritik aus M40, die den Belegdaten-Block davon abhält, der Konfiguration zu *folgen*, traf sie als Spaltenauswahl und nicht als **Angebot**. Drei Mandanten haben keinen Typ (`EDITIONLINGERI`, `SYSTEM`, `WOC`) und bekommen `200` mit leerer Liste — kein Platzhalter, keine leere Auswahl. Gemessen in **M48**: **0,534 ms** bei 40 Typen, `ref` über `MandantIDSortIndexBAMTYpeIDX` mit `Using index` und **ohne `filesort`**, weil der Index Filter *und* beide Sortierschlüssel trägt. Der Befund, der nicht im Auftrag stand: **`MessageBAMTypeSortIndex` ist nicht eindeutig** — bei `VOTG` tragen 2002 und 2011 denselben Index, und ohne die Typnummer als zweiten Schlüssel zeigten zwei Aufrufe dieselbe Auswahl verschieden. Sein Isolationstest wählt **`NEXANS` gegen `SUTTONS`** statt der Vorlage `VOTG`/`SUTTONS`, und der Grund steht dabei: Die Konfiguration von `SUTTONS` ist eine **echte Teilmenge** der von `VOTG` — ein Leck bliebe in einer Richtung unsichtbar. **Die Oberfläche:** Das Feld sitzt im seit Schritt 3 reservierten Platz der Kopfzeile (`--dichte-suchbereich`), **ohne Navigationseintrag** — es steht auf jeder Seite, ein Menüpunkt daneben wäre eine zweite Tür in denselben Raum. Gesucht wird **auf Eingabe**, nicht beim Tippen (M35: 8,66 s beim Jahresfenster für den schlimmsten Wert), **kein Aufklappmenü**, und die Belegart ist Verfeinerung mit Vorgabe *kein Typ* (M36: +1,5 bis +4 %). Die Begriffe stehen als **Marken** über der Trefferliste, mit `UND` verknüpft, höchstens **acht** als Schutzgeländer, Schlüssel `(typ, wert)` und **niemals der Wert allein** (M37). Die Marke ist damit eine **gemeinsame Bauform** geworden und aus [`bam-werte.md`](bam-werte.md) §11a nach [`visuelles-konzept.md`](visuelles-konzept.md) §5 gewandert — mit der Bedienbarkeit als **Schalter**, der nicht per Voreinstellung anspringt: Die Werte-Marke im Detail bleibt **kein Knopf**. Eigene Route **`/suche`**, Zustand vollständig in der URL, die Begriffe als **wiederholter** `begriff`-Parameter — und das ist keine Geschmacksfrage: Eine Liste in *einem* Parameter brauchte ein Trennzeichen, und für jedes wäre ungemessen, ob ein BAM-Wert es enthält (Regel Q4, dieselbe Falle, die der Pflicht-Doppelpunkt schon einmal umgangen hat). Die Trefferliste zeigt **dieselben Zellen** wie die Nachrichtenliste — ausgeführt statt nachgebaut — plus **„Treffer"** (der **Typ**, nicht der Wert; bei mehreren `+N` über *verschiedene* Typen) und den **Kettenhinweis** (96,87 % der Wurzeln tragen BAM-Werte gegen 2,42 % der Kinder, M26‑1b — ohne ihn hält sich der Nutzer für fertig). Über der Liste stehen **Trefferzahl und Zeitfenster zusammen**, die **Varianten der Normalisierung** (keine stille Korrektur; die Leerzeichen-Fassung wird gesucht und nicht gemeldet) und die **Nulltreffer-Zeile** *„Mit diesem Begriff: 0. Ohne ihn: 12."*, die keine zusätzliche Abfrage kostet und nur erscheint, wenn wirklich ein Begriff dazugekommen ist. Das Zeitfenster ist **sichtbar und verstellbar**, und keines seiner drei Bedienelemente rechnet gegen die Browseruhr: „Auf ein Jahr erweitern" rechnet **vom `bis` aus der Antwort** zurück, mit **365 Tagen statt einem Kalenderjahr**, weil ein im Browser gerechnetes Kalenderjahr am 29. Februar einen Tag über die Grenze aus L1 liefe. **§14 hält den einen Befund fest, den ein Nutzer als Fehler lesen wird und der keiner ist:** Eine **Lieferschein-Suche ohne führende Null** wirkt unvollständig — Typ 9006 ist mit 94,21 % durch die Schwelle gefallen und steht nicht in `bam_sollaenge`, die Suche findet dort **1.642 statt 4** Treffer (M43‑4). Das ist eine offene Entscheidung des Auftraggebers ([`bam-sollaengen.md`](bam-sollaengen.md) §8) und wird **nicht** durch einen Sonderfall im Frontend geheilt — eine zweite Kuratierungsstelle liefe der ersten hinterher. **Teil 2b (13.08.2026) — das Backend:** `GET /api/bam/suche`. Wer eine Belegnummer hat, findet die Nachrichten, auf denen sie steht: **auch ohne die führende Null** und **auch mit mehreren Nummern gleichzeitig**. Die Parameterform ist `begriff=<typ>:<wert>`, wiederholbar, **der Doppelpunkt ist Pflicht** — ohne ihn müsste die Anwendung raten, ob eine führende Ziffernfolge ein Typ ist oder Teil des Werts, und ob ein BAM-Wert selbst einen Doppelpunkt tragen kann, ist **nicht gemessen**; mit Pflichttrenner und Teilung am *ersten* Doppelpunkt ist die Frage gegenstandslos statt nach Regel Q4 beantwortet. **Zwei begründete Abweichungen von den Leistungsregeln stehen im Zentrum:** Das Pflicht-Zeitfenster hat die Vorgabe **30 Tage statt 24 Stunden** (L1) — der Nutzer mit einer Belegnummer hat *kein Datum*, und ein Tagesfenster fände beim schlimmsten gemessenen Wert **279 von 234.159** Nachrichten, verändert also nicht den Preis, sondern die Antwort. Und es gibt **keine Mindestlänge** (L5): E6 belegt, dass die Kostengröße die **Trefferzahl** ist und nicht die Zeichenlänge, M38, dass die Werte je Typ zwischen 1 und 35 Zeichen lang sind — eine feste Mindestlänge von vier machte neun Typen unsuchbar. Was trägt, sind Zeitfenster und **hartes Limit 50**, ohne Cursor. Die **Normalisierung** bildet die Suchvarianten *vor* dem Statement aus [`bam-sollaengen.md`](bam-sollaengen.md) — roh, auf die Sollänge des Paares `(Mandant, Typ)` aufgefüllt, und für zwei Paare zusätzlich mit **führendem Leerzeichen**; drei Regeln halten die Menge klein (verschiedene statt aller Sollängen, nur auffüllen nie kürzen, doppelte entfernen), und die Obergrenze ist bei `NEXANS` gezählt: **fünf** Fassungen, erreichbar nur mit ein- bis zweistelliger Eingabe. **Die Varianten stehen in der Antwort** — keine stille Korrektur —, mit genau einer Ausnahme samt Begründung: die Leerzeichen-Fassung wird gesucht und nicht gemeldet, weil der Nutzer sie weder in seiner Eingabe noch im Wert sieht. Die Abfrage ist ein **Selbstjoin** je Begriff, **ohne `STRAIGHT_JOIN`** (mit ihm bricht die Laufzeit um Faktor 219 bis 1.094 ein) und **nicht** in der `HAVING COUNT(DISTINCT …)`-Bauform; die Anzeigespalten hängen **über** der Deckelung und nicht daneben. Gemessen ist alles in **M47** gegen den von jOOQ *gerenderten* Text: Normalfall **1,1 ms**, schlimmster Wert **1,656 s** über 30 Tage und **8,940 s** über ein Jahr, und der Optimierer steigt in **jeder** Konstellation über den seltensten Begriff ein — auch wenn dieser an fünfter Stelle steht. Der Nachtrag beantwortet zwei ausdrücklich offene Fragen: **das 30-Tage-Fenster fängt beide Bösfälle der Verundung** (K3b 5.275 → **882 ms**, K5c 926,7 → **6,9 ms**), und eine wirkungslose Suchvariante kostet **rund 0,1 ms** — womit die offene Frage zu `IBIS`/1 und `SUTTONS`/2000 keine Leistungsfrage mehr ist. Der Befund, der in keiner Zeile des Auftrags stand: **Die Anzeigespalten neben statt über der Deckelung kosten Faktor 1,48 — bei identisch gutem `EXPLAIN`**, derselbe Fehler wie in Teil 1 (dort Faktor 18,4). Der Isolationstest ist der erste, dessen Gegenprobe **ohne `404`** auskommen muss (§7; die Vorlage in [`mandantentrennung.md`](mandantentrennung.md) §5 ist entsprechend ergänzt), und er prüft zusätzlich, dass die **Abschneidung nach dem Mandantenfilter** gezählt wird |
| [`rohdaten.md`](rohdaten.md) | Schritt 8, die Vorgabe | **Was gebaut wird und warum** — die zehn Entscheidungen des Rohdatenzugriffs, aus denen Backend und Oberfläche folgen. **Die Anzeige ist der Regelfall, nicht der Download**, und zwar als Rohtext: Die aufbereitete Zerlegung in Segmente bleibt ausgeschlossen (`PROJEKTBESCHREIBUNG.md` §9) — bemerkenswert dabei, dass das Altwerkzeug sie **hat** und alle vier Formate `disabled: true` stehen. **Der Beschnitt bei Protokollen hängt an der Rolle und nicht an einem Flag**, und er ist in zwei Fällen strenger als das Altsystem: Die Marken stehen in Werten, die **vom Partner** kommen, und wären sonst von außen zu setzen. **Vier benannte Zustände**, von denen keiner ein leeres Feld ist — „Datei nicht vorhanden" und „Ablage nicht erreichbar" bleiben ausdrücklich getrennt. Dazu der Abrufweg (SOAP `RETRIEVE`, ZIP-Anhang, keine Spiegelung zwischen Ablagen), der **Gleichlauf** von Anzeige und Download, drei Ereignisarten im `audit_log` mit der **Fassung** als Kern — und **Entscheidung 6 in ihrer korrigierten Fassung vom 18.08.2026**: Artefakte hängen an der Zeitleiste, die alte Fassung steht im Kasten daneben |
| [`rohdaten-backend.md`](rohdaten-backend.md) | Schritt 8, Teil Backend | Die drei Endpunkte unter `…/{messageId}/dateien` — Liste, Anzeige, Download. **Die `artefaktId` ist die sicherheitskritische Zeile:** `<MessageActionID>-<MessagePropertyName>`, und sie enthält **niemals** die GUID und niemals die Ablagenkennung — nähme ein Endpunkt einen Verweis entgegen, wäre er ein offener Proxy vor einer Produktionsablage, genau wie im Altsystem. **Der Proxy ist kein Bytestrom-Proxy:** Der Filestore spricht ausschließlich SOAP und liefert die Datei als ZIP-Anhang; das Backend liest, entpackt, prüft auf Binärbytes, dekodiert nach `ISO-8859-1` (gemessen, M61) und beschneidet für `MANDANT` bei Protokollen — **in dieser Reihenfolge**, weil der Beschnitt sonst Marken in Zeichenmüll suchte. **Fünf Zustände**, und die Anzeige antwortet in allen mit `200`: „Protokoll ohne Marken" ist bei `FTPSender` der Normalfall und darf sich nicht wie „Nachricht gibt es nicht" anfühlen. **Der Gleichlauf ist ein Codepfad und nicht zwei** — es gibt keinen Weg, auf dem ein Mandantennutzer die vollständige Protokolldatei bekommt; die Binärprüfung steht deshalb *im* beschnittenen Zweig und nicht davor. Dazu: warum SAAJ und warum der `jakarta`-Zweig (M72), die vier Grenzen, drei Ereignisarten im `audit_log` mit der **Fassung** als Kern, die Messungen nach L7 — und **fünf benannte Abweichungen** von [`rohdaten.md`](rohdaten.md), darunter die tragendste: Die Liste trägt **keinen lesbaren Schrittnamen**, weil die Namensauflösung im Paket `message` liegt |
| [`rohdaten-frontend.md`](rohdaten-frontend.md) | Schritt 8, Teil Frontend (nachgebessert am 18.08.2026) | Die **Ziele an der Zeitleiste** des Nachrichtendetails und die **eigene Route** `/nachrichten/{id}/dateien/{artefaktId}`. **Die Beschriftung ist der Punkt, an dem sich das Feature entscheidet:** Die Liste liefert nur Schrittnummer und technische Familie — der lesbare Name entsteht hier, durch Verbindung mit der Schrittfolge über `position`, und für die **55,98 %** ohne auflösenden Schritt (M57) steht `Schritt <n> · <Familie>`. **Nichts wird geraten**, die Familie wird nirgends übersetzt, und **nichts auf Schritt `0` trägt eine Schrittnummer** — dort steht die Familie allein, denn die Zeitleiste führt Schritt `0` nicht. **Der eigene Block ist entfallen:** Je Schritt hängen bis zu zwei Zeichen an seiner Zeile, der Eingang steht in einer eigenen Zeile darüber, und ein Klick auf den Schrittnamen führt in seine Gruppe im Eigenschaftenblock. **Kein Artefakt wird dabei unerreichbar** — auch nicht das, dessen Schritt keine Zeile hat. **Der Inhalt ist ein Textknoten in einem `<pre>` und niemals HTML** — eine EDI-Datei kann gültiges HTML enthalten, und der Inhalt kommt vom Partner; ein Test stellt genau so einen Inhalt und weist nach, dass kein einziges Element entsteht. **Ein Kind, kein Element je Zeile** (M60: 609.995 Byte), daraus folgend **keine Zeilennummern**. **Umbruch statt waagerechtem Bildlauf**, und der Grund ist gemessen: 36 von 206 Artefakten tragen überhaupt kein Zeilenende (M61). **Die vier Zustände tragen je einen eigenen Text und keiner ist ein leeres Feld**; der Unterschied zwischen „nicht vorhanden" und „nicht erreichbar" wird über das **Angebot** getragen — nur einer bekommt „Erneut versuchen" — und nicht über Farbe, denn Rot gehört dem `MessageStatus`. **Der Download-Knopf steht nur dort, wo der Endpunkt liefert** (Entscheidung 9). Dazu vier benannte Abweichungen und die offenen Punkte, darunter die ausstehende Sichtprüfung |

Die folgenden Dateien entstehen laut Plan:

| Datei | Entsteht in | Inhalt |
|---|---|---|
| ~~`verkettung.md`~~ | ~~Schritt 6~~ | **Angelegt** — siehe oben. Die Zeile bleibt hier stehen, damit erkennbar ist, dass sie den Plan erfüllt und nicht daneben entstanden ist |
| ~~`prozessschritte-uebersetzung.md`~~ | ~~Schritt 5~~ | **Entsteht nicht.** Angekündigt war eine Zuordnungstabelle `SOSActionServiceProperties` → Klartext. Sie ist durch [M19](messungen-schritt5.md#m19--wie-groß-müsste-die-zuordnungstabelle-sein) überholt: Über den ganzen dichten Monat stehen hinter 103.402 namenlosen Schritten **vier** Marken, **drei davon sind bereits lesbarer Klartext** (`EERP received`, `Message has been sent`, `EERP pending`). Zu übersetzen bliebe genau eine — `FTPSender` —, und ausgerechnet die löst anderswo auf **25 verschiedene** `SOSActionName` auf; eine Tabellenzeile müsste diese 25 auf einen Sammelbegriff eindampfen und wäre damit nicht die fehlende Übersetzung, sondern eine gröbere. An ihre Stelle tritt die zweite Stufe der Namensauflösung in [`nachrichtendetail.md`](nachrichtendetail.md) §2, die den **echten** Namen liefert und keine gepflegte Zeile braucht. Die Zeile bleibt hier stehen, statt still zu verschwinden — wer die Vorschau in einem älteren Stand liest, soll hier finden, warum die Datei fehlt |
| ~~`bam-suche.md`~~ | ~~Schritt 7, Teil 2b und 3~~ | **Angelegt in Teil 2b, in Teil 3 vervollständigt** — siehe oben. Die Zeile bleibt hier stehen, damit erkennbar ist, dass sie den Plan erfüllt und nicht daneben entstanden ist. **Zwei Abweichungen von der Vorschau sind benannt und begründet:** Der Plan nennt „Limit und **Mindestlänge**" — das Limit steht, die Mindestlänge ausdrücklich **nicht** (E6, M38, dort §1). ~~Und die Datei ist nach Teil 2b **fertig für das Backend**, aber nicht für Teil 3: Suchfeld, Chips und Trefferliste kommen dort dazu.~~ ✔ **Am 13.08.2026 nachgeholt** (§10 bis §14) — samt eines zweiten Endpunkts (`GET /api/bam/typen`), den die Vorschau nicht kannte und den die Auswahl neben dem Feld verlangt. **Teil 1 ist angelegt** und heißt [`bam-werte.md`](bam-werte.md); er steigt über die `MessageID` ein und ist deshalb bewusst eine eigene Datei. **Teil 2a ist ebenfalls angelegt** und heißt [`bam-sollaengen.md`](bam-sollaengen.md) — die Kuratierung, ohne die die Normalisierung kein Maß hätte; sie beschreibt eine Tabelle und keinen Endpunkt und bleibt deshalb getrennt |
| ~~`rohdaten-download.md`~~ | ~~Schritt 8~~ | **Entstanden als drei Dateien statt einer** — [`rohdaten.md`](rohdaten.md) (die Vorgabe, mit den zehn Entscheidungen), [`rohdaten-backend.md`](rohdaten-backend.md) und [`rohdaten-frontend.md`](rohdaten-frontend.md); siehe oben. Die Zeile bleibt stehen, damit erkennbar ist, dass sie den Plan erfüllt. **Der Name der Vorschau ist überholt**, und das ist kein Zufall: Der Plan sah einen reinen Download-Proxy vor. Gebaut ist mehr — **die Anzeige ist der Regelfall**, Protokolle gehören dazu, und ein Proxy ist es nicht, weil die Ablage SOAP spricht und ein ZIP liefert. Aus demselben Grund heißt das Backend-Paket `payload` nicht mehr passend ([`rohdaten-backend.md`](rohdaten-backend.md) §11, Punkt 2) |
| [`prozess-katalog.md`](prozess-katalog.md) | Schritt 9b, die Vorgabe | **Was gebaut wird und warum** — einundzwanzig Entscheidungen E1 bis E21 und eine Liste verworfener Möglichkeiten — **E14 bis E21 sind am 21.08.2026 dazugekommen** (Bestandslauf, Zeilenpflege und Filter), und **E14 kehrt eine der verworfenen Möglichkeiten um**. **Der Katalog macht aus etwas Lesbarem etwas Gruppierbares**, und das ist seine einzige Aufgabe; Abnehmer ist ausschließlich Schritt 10. **Zwei kuratierte Felder statt vier** (E1 — Standort und Belegart entfallen, das löst nebenbei die Namenskollision mit den BAM-Belegarten aus Schritt 7), **keine Tabelle `partner`** (E2), **der Mandantenbezug fällt aus dem Join** und braucht keine Spalte (E3, belegt mit `projekte_mit_mehreren_mandanten = 0`). **E4 ist die tragende Entscheidung:** Drei Bedeutungen aus zwei vorhandenen Feldern — ein *leerer Partner mit Status gepflegt* heißt „hingesehen, es gibt nichts", und ohne diese Speicherbarkeit erreichte der Fortschritt nie sein Ende. **Auch tote Prozesse werden kuratiert** (E5): 765 von 1.503 tragen keine einzige Nachricht, und genau deshalb — ein Monitoring muss auch zeigen, was *nicht* passiert ist, und „von X kam seit acht Wochen nichts" braucht ein X. Dazu die zwei Heuristikregeln mit ihrer Begründung (die Zweiteilung aus M75 ist eine über **Zeichensetzung**, nicht über Ableitbarkeit), die **Korrektur an der Massenzuordnung** (E11 — der Hebel greift, aber je Mandant für ein anderes Feld; die Korrektur ist am 20.08.2026 auf ihre richtige Fundstelle umgehängt worden, den **Implementierungsplan** und nicht `PROJEKTBESCHREIBUNG.md` §4.4), das **bewusste Überschreiben gepflegter Zeilen** (E12) und der **wiederholbare** Lauf statt des geplanten einmaligen Imports (E13). **E6 ist am 20.08.2026 eindeutig gemacht worden:** „Projekt und Name" ließ beide Lesarten zu, sortiert wird nach **`ProjectID`, dann `ProcessID`** — beides Schlüssel, während `ProcessName` nicht eindeutig ist und eine Liste ohne Paginierung damit keine feste Reihenfolge hätte. **Die Richtung steht seither gezählt neben den Partnerzahlen** (§3.5): **1.020 von 1.490** Prozessen, 68,5 %, bei `NXHBE` alle 17 und bei `VOTG` **keiner** — das Letzte ist kein Befund, sondern die Entscheidung gegen den `SOSName`-Weg. §10 führt vier offene Punkte, der vierte ist bis auf `EDITIONLINGERI` beantwortet **Nachtrag 26.08.2026 — E22 bis E24, die Vorschlagsübernahme** (§4, hinter E21): Ein Knopf setzt alle offenen Zeilen mit einem **Partner**vorschlag aus Regel A oder Regel B auf *gepflegt*. **Er kopiert keinen Feldwert** — Partner und Richtung stehen längst in der Zeile, die Heuristik hat sie beim Lauf geschrieben; Übernehmen ist eine Statusänderung. **E22 ist die tragende der drei:** Zeilen mit Herkunft `KEINE` bleiben offen, **auch wenn sie eine Richtung tragen** — bei `NEXANS` sind das 224 Prozesse, und „gepflegt mit leerem Partner" hieße nach E4 „hingesehen, es gibt keinen". Ein Knopf, der 224 Behauptungen erfindet, ist schlimmer als einer, der 224 Zeilen liegenlässt. **E23** hält die Menge im Backend: keine Liste von `ProcessID` auf der Leitung, und der Browserfilter „nur mit Nachrichten" wirkt nicht — eine Regel an zwei Stellen driftet. **E24** ist E12 in stärkerer Form: nicht zwei Wege mit einem Statement, sondern **eine Lesung**, über die gezählt und geschrieben wird — es kann nichts driften, weil es nichts gibt, was auseinanderlaufen könnte. §9 führt drei neue verworfene Möglichkeiten, §10 zwei neue offene Punkte: **es gibt keinen Weg von *gepflegt* zurück nach *offen*** (gegen den Code geprüft), und „per Regel übernommen" ist von „von Hand kuratiert" in der Zeile nicht mehr unterscheidbar — nur noch im `audit_log` |
| [`prozess-katalog-backend.md`](prozess-katalog-backend.md) | Schritt 9b, Teil Backend | `V6__process_catalog.sql`, seit dem 21.08.2026 dazu `V8__bestandsspalte.sql`, und fünf Endpunkte unter `/api/katalog`. **Der Knopf `vorschlagen` fährt drei Schritte, und der dritte ist bewusst weniger vorsichtig als der zweite** (§3.5): Die Heuristik schont gepflegte Zeilen, der **Bestandslauf** schreibt über alle — E13 schützt Kuratierung, nicht Beobachtung. Er ist deshalb der einzige Schreibweg ohne Upsert: Ein `INSERT … ON DUPLICATE KEY` müsste die `NOT NULL`-Spalten mitliefern und überschriebe genau das, was er in Ruhe lassen soll. `traegt_nachrichten` trägt **drei** Zustände — `null` heißt „noch nie geprüft" und wird nicht auf `false` abgebildet. **Regel L7 war mit M83 nicht erfüllt und ist es erst mit M84:** M83 misst den von Hand getippten Text, der gerenderte war ungemessen; er erzeugt denselben Plan und kostet 23,2 ms für `NEXANS`. **Die Heuristik ist eine Klasse ohne Spring und ohne Datenbank** — Zeichenketten hinein, ein Vorschlag oder nichts heraus; die Regeln stehen unter offenen Punkten und dürfen sich ändern, ohne einen Service anzufassen. **Zwei Lesarten der Vorgabe waren zu entscheiden, und beide sind an den Zahlen aus §3.5 belegt statt gewählt:** Der Nummernpräfix ist *Bedingung* von Regel A (ohne ihn bekämen 14 von 17 `NXHBE`-Prozessen einen Vorschlag, den die Festlegung nicht kennt) — mit ihm ergibt die Regel 509 + 378 = **887**, exakt die dort ausgewiesene Zahl, und die Gegenprobe über die 322 ohne Vorschlag geht ebenso auf. „Ohne Trennzeichen" ist dagegen *Zweck* und nicht Bedingung von Regel B. **Und ein Regressionstest gegen den echten Bestand (`HeuristikBestandDbIT`) war beim ersten Lauf rot — der Befund war nicht die Zahl, sondern eine erfundene Bedingung im eigenen Code:** Die erste Fassung von Regel B verlangte vor dem Ankerwort einen Kleinbuchstaben, was 26 Prozesse kostete, die dort ein zweibuchstabiges Kürzel in Großschreibung tragen. Nach der Korrektur trifft Regel A die Projektion aus §3.5 auf den Prozess genau, Regel B liefert **280 statt 281** — sechs Prozesse tragen den Anker gar nicht, dafür trifft sie fünfmal bei `EDITIONLINGERI`, wo die Projektion sie nicht erwartet hatte. **Die Summe stimmt dabei auf eins genau, aus zwei Fehlern**, die einander fast aufheben — der Test zählt deshalb je Mandant und nicht nur die Summe. Nebenbei fällt die Zahl an, die in keinem Dokument steht: Die Heuristik füllt für **1.020 von 1.490** Prozessen eine **Richtung** (68,5 %), bei `NXHBE` für alle 17 und bei `VOTG` für keinen einzigen. **`process_catalog` ist die erste eigene Tabelle, die wirklich gegen `GlassfishDB` gejoint wird** — die Sortierungszeile trägt ab hier statt vorzusorgen. **Zwei Fallen sind zugeschnappt und stehen ausgeschrieben:** ein Stapel-Upsert, dessen feste Statuswerte ebenfalls Bindeplätze sind (jOOQ füllt still mit `null` auf, jede Zeile wäre um Spalten verschoben geschrieben worden), und `@Transactional`, das **nur** den Schreib-Kontext bindet — das Nachlesen über den Lese-Pool meldete `OFFEN`, obwohl `GEPFLEGT` geschrieben war. **Sechs benannte Abweichungen von Festlegung und Auftrag — dazu eine siebte, die noch am selben Tag zurückgenommen wurde:** `DATETIME(3)` statt `timestamp` und die Rollengrenze `ADMIN`, die der Auftrag nicht nennt, bleiben; **die Sortierung nach `ProjectName` ist gefallen**, nachdem E6 eindeutig gemacht wurde (`ProjectID`, dann `ProcessID`) — nicht weil die Auflösung falsch war, sondern weil die Vorgabe mehrdeutig war. **Damit ist der ausgelieferte Text an genau dieser Stelle nicht mehr der gemessene**, und M80 sagt das ausdrücklich **Nachtrag 26.08.2026 — der sechste Endpunkt, ohne Migration.** `POST /api/katalog/vorschlaege-uebernehmen` (§3.6, §4): Lesung mit `INNER JOIN` auf `process_catalog` statt `LEFT JOIN` — nicht als engere Bedingung, sondern als ehrlichere Schreibweise, und **M93-5b misst nach, dass der `LEFT JOIN` dieselben Zahlen liefert**. Das Schreiben ist **ein einziges `UPDATE` mit `IN`-Liste** (höchstens 733 Bindeplätze, kein `batch` — genau dort war die Bindezähler-Falle) und **bewusst kein Upsert**: die Zeile existiert notwendigerweise, ein `INSERT`-Zweig wäre unerreichbar. **Damit ist er der zweite Schreibweg ohne Upsert und der erste kuratierende** — und `schreibende_statements_sind_upserts` ist ein zweites Mal geschärft worden: Er führt die **fünf Schreibwege jetzt namentlich** statt sie über ein Textmerkmal zu trennen, drei mit Upsert und zwei ohne, dazu ein Riegel, der einen sechsten rot werden lässt. Dieselbe Begründung wie beim ersten Mal: eine Ausnahme, die nur durch Weglassen entstünde, wäre beim nächsten Umbau wieder da. **Die gemeldete Zeilenzahl geht nicht nach außen** — nach außen geht die Größe der gelesenen Liste, damit Vorschau und Ausführung dieselbe Zahl nennen; weichen sie ab, steht eine `WARN`-Zeile mit beiden im Protokoll, denn Lesung und Schreiben laufen auf verschiedenen Verbindungen. **Eigene Ereignisart `KATALOG_VORSCHLAEGE_UEBERNOMMEN`**, weil das `audit_log` der einzige Ort ist, an dem „per Regel" und „von Hand" danach noch auseinandergehalten werden. **Sechs neue Abweichungen (§9, 12 bis 17)**, darunter zwei Signaturen, die vom Auftragstext abweichen, und eine Regelnummer, die es nicht gibt: **„L15" steht in `DEVELOPMENT_GUIDELINES.md` nicht** — dort stehen L1 bis L10; der Sache ist gefolgt worden, der Nummer nicht |
| [`prozess-katalog-frontend.md`](prozess-katalog-frontend.md) | Schritt 9b, Teil Frontend | Der Administrationsbereich und die Katalogpflege — **die einzige schreibende Ansicht des Werkzeugs**. **Der Zustand „kein Zugriff" wird am Problemtyp erkannt und nicht am Statuscode:** `403` ist dreifach vergeben (`zugriff-verweigert`, `csrf-token-ungueltig`, `kein-mandant-gewaehlt`), und eine Statusprüfung zeigte allen dreien dieselbe Meldung — zwei davon falsch. Er ist weder Fehler (nichts ist kaputt) noch Leerzustand (es gibt etwas, er darf es nur nicht sehen). **Die Übersicht prüft die Rolle bewusst nicht:** Sie ruft kein Backend auf und könnte es nicht belegen; eine Prüfung gegen `role` wäre eine Berechtigungsentscheidung im Browser. **§7 des Frontends gilt hier zum ersten Mal für eine sehr lange Liste**, und die Umsetzung besteht aus dem, was nicht dasteht: kein zweites `overflow-y-auto` — und ausdrücklich auch **kein `overflow-x-auto` darüber**, denn das stuft `overflow-y` auf `auto` hoch, macht den Container selbst zum Scrollbereich und lässt die klebende Kopfzeile an ihm statt am Fenster kleben. Deshalb steht dort **kein `<Table>` aus `components/ui`**, und deshalb `border-separate` statt `border-collapse`: Mit `collapse` gehören die Rahmen der Tabelle, und die Kopfzeile ließe ihren Trennstrich zurück. **Keine Farbe in der ganzen Zeile** — die vier Statusfarben sind fachlich an den Nachrichtenstatus vergeben, die Unterscheidung liegt vollständig im Wort. **Der eine tragende Gedanke der Bearbeitung: „nichts" ist an beiden Feldern eine Wahl und kein Zustand, in den man fällt.** Der Partner hat eine Leeren-Schaltfläche, und genau wenn das Feld leer ist, sagt ein Satz, was das Speichern dann bedeutet — „hingesehen, es gibt keinen" ist die einzige Pflege, die ein toter Prozess je bekommt; die Richtung hat drei Knöpfe statt zwei, weil Abwahl beim zweiten Druck dasselbe sagt und es niemandem sagt. Das Formular klappt **unter** der Zeile auf: Eingabefelder in den Zellen wären am schmalen Fenster für die Richtung gar nicht erreichbar, ein Dialog nähme die Nachbarzeilen weg, und die Zeile zu ersetzen nähme den bisherigen Stand. **Der offene Zeilenschlüssel steht nicht in der URL** — er beschreibt eine begonnene Eingabe, und die Hälfte, auf die es ankommt, lässt sich ohnehin nicht teilen. **Der Lauf bekommt keinen Fortschrittsbalken**, sondern die verstrichene Zeit: Ein Balken verspricht bekannten Fortschritt, und der Aufruf hat keinen; die Dauer bleibt danach stehen, weil sie **die Messung** ist, die der Handlauf verlangt — **genommen am 24.08.2026 als M85: 509,4 ms beim ersten Druck über 733 Zeilen, also 5,1 % der Entscheidungsschwelle; die 10–25 s je COMMIT aus dem Backend-Dokument waren ein Sonderfall und sind als solcher benannt**. **Der Fortschritt ist eine Zahl und kein Balken**, obwohl es dort sehr wohl bekannten Fortschritt gäbe: Bei `VOTG` stünde er lange bei einem Zehntel, und eine breite, fast leere Fläche liest sich als Alarm. **Fortschritt, Hinweis und Projektauswahl lesen die volle Liste und nicht die gefilterte** — über der gefilterten wären alle drei falsch, sobald `nurOffene` gesetzt ist; das kostet dann eine zweite Abfrage über denselben Endpunkt und bleibt trotzdem ohne eigene Zähl-Abfrage. **Zwölf benannte Abweichungen**, darunter zwei, die keine Arbeit waren, sondern ein Befund: `/verwaltung` steht im ganzen Repository an genau einer Stelle — im Kasten, der ihn meldet —, und `PROJEKTBESCHREIBUNG.md` §7 war seit dem 20.08.2026 bereits angeglichen **Nachtrag 26.08.2026 — der zweite Dialog (§7a).** „Vorschläge übernehmen", nach dem Muster der Massenzuordnung. **Der Knopf trägt keine Bedingung „gibt es überhaupt welche"** — die beantwortet die Vorschau dort, wo die Regel aus E22 lebt; stünde sie zusätzlich im Browser, stünde sie zweimal. Die Vorschau wird **beim Öffnen im Ereignis** geholt und nicht in einem Effekt, Schließen verwirft sie. **Der Satz ist dreiteilig, und der dritte Teil ist Pflicht:** Ohne ihn liest ein Administrator bei `NEXANS` „509" über 733 Zeilen und sucht die fehlenden 224 in einem Fehler statt in E22. **Die Null bekommt ein Wort und keine Ziffer** — „keinen aus Regel B" ist eine Aussage, „0 aus Regel B" eine Tabelle, die in einen Satz gerutscht ist. Ein vierter Satz erscheint, sobald „nur mit Nachrichten" gesetzt ist, **und auch bei null Treffern** — sonst verdächtigt der Nutzer den Filter. **Keine zweite Rückfrage und nichts fett:** Übernehmen überschreibt nichts, und eine Hervorhebung ohne Verlust dahinter gewöhnt den Leser daran, Hervorhebungen zu überlesen. **Die Partnerliste wird ausdrücklich nicht invalidiert**, und das ist gegen die Erwartung: `GET /api/katalog/partner` filtert nicht auf den Pflegestatus, ein Regelvorschlag steht also schon **vor** der Übernahme in der Auswahl — eine Invalidierung wäre eine Anfrage über 733 Zeilen für eine Antwort, die sich nicht geändert haben kann |
| [`benutzerverwaltung.md`](benutzerverwaltung.md) | Schritt 9a, die Vorgabe | **Was gebaut wird und warum** — einundzwanzig Entscheidungen E1 bis E21 und eine Liste verworfener Möglichkeiten — **E19 bis E21 sind am 21.08.2026 dazugekommen** (§7a, Oberfläche) und ~~**noch nicht gebaut**; E20 verlangt zusätzlich eine Backend-Änderung~~ — ✔ **alle drei am 24.08.2026 gebaut**, einschließlich der Backend-Änderung zu E20 ([`benutzerverwaltung-frontend.md`](benutzerverwaltung-frontend.md)). **Die Begründung ist am 20.08.2026 ausgetauscht und nicht ergänzt worden** (E1): Nicht „ohne Datenbankzugriff betreibbar" — der Entwickler *ist* der Betreiber und hat einen SQL-Client —, sondern über zwanzig externe Nutzer, die nie einen bekommen. **Die Nutzerverwaltung ist mandantenfrei** (E2), und das ist nicht die bequemere, sondern die richtige Liste: Ein ADMIN-Konto trägt eine wirkungslose Mandantenzuordnung und stünde gefiltert unter einem Mandanten, für den es nicht gilt. **Regel M4 wird zur Rollengrenze** (E3) — jeder Endpunkt gibt einem MANDANT-Nutzer `403`, *auch auf dessen eigenes Konto*. **E5 ist die tragende Entscheidung:** Alle Sitzungen eines Kontos werden bei jedem der fünf schreibenden Vorgänge verworfen — ohne sie wäre die Zusage nicht eingelöst, mit der dieses Projekt JWT abgelehnt hat. Die **eigene** Passwortänderung verwirft dagegen nur die übrigen (E6). **E7 verlangt die Verifikation vor der ersten Zeile Code** — gefahren als M81, alle vier Punkte halten. Dazu **kein Löschen** (E8), die n:m-Pflege (E9), die unentfernbare letzte Zuordnung für **beide** Rollen (E10), die abgelehnte Herabstufung ohne Zuordnung (E11 — das Bootstrap-Konto ist der Anlass), der zweistufige Selbstschutz mit *nutzbar heißt aktiv und nicht gesperrt* (E12), das **getippte** Einmalpasswort (E13), die Trennung von `SPERRE_DURCH_ADMIN` und `KONTO_GESPERRT` (E14 — ein Angriff und ein Verwaltungsakt gehören nicht in dieselbe Zeile), **keine eigene Ereignisart für den Entzug** (E15), keine Paginierung (E16), die letzte Anmeldung aus dem `audit_log` statt aus einer neuen Spalte (E17) und der Fall von `download_allowed` (E18) |
| [`benutzerverwaltung-backend.md`](benutzerverwaltung-backend.md) | Schritt 9a, Teil Backend | `V7__benutzerverwaltung.sql` und sechs Endpunkte unter `/api/admin/users`. **Die Migration tut zwei Dinge**: `download_allowed` fällt (E18, ein **Vertragsbruch aus Schritt 3**, datiert vermerkt — geprüft ist, dass das Frontend das Feld nirgends *liest*), und `locked_by_admin` kommt. **Die zweite Spalte war eine Lücke in der Vorgabe und ist gemeldet worden**: Keine der drei verbindlichen Dateien sagt, wo die administrative Sperre steht, und `locked_until` mitzubenutzen ist am Bestand **nachweisbar defekt** — `setzeFehlversuche` schreibt die Spalte bedingungslos, ein einziger falscher Anmeldeversuch löschte damit eine Admin-Sperre. Das ist E14 auf Datenebene fortgeschrieben. **Der Sitzungsentzug ist Bauform A** (M81): über `SPRING_SESSION.PRINCIPAL_NAME`, kein Generationszähler, keine Abfrage je Anfrage. **Die Reihenfolge in `passwortAendern` ist der Punkt, an dem E6 kippen kann** — die Sitzungs-ID wird gelesen, *bevor* sie erneuert wird, sonst verwirft der Entzug genau die Sitzung, die bleiben soll. **Sechs benannte Abweichungen**, darunter drei Lücken, die keine Datei füllt: die neue Spalte, der Pfad `{id}/tenants` (drei andere Dateien führten `{benutzername}/mandanten`) und **neun Statuscodes**, die nirgends festgelegt waren — `409` und nicht `403` für die vier Zustandsfälle, weil `403` hier „die Rolle reicht nicht" heißt. **Der Entzug trifft auch den Handelnden**, und das ist festgehalten statt weggebaut: E5 ist eine Regel ohne Fallunterscheidung |
| [`benutzerverwaltung-frontend.md`](benutzerverwaltung-frontend.md) | Schritt 9a, Teil Frontend | Die Oberfläche der Benutzerverwaltung — **und damit ist Schritt 9 gebaut**. Sie beantwortet vor allem eine Frage, und daran entscheidet sich fast alles darin: **„Warum kommt der da nicht hinein?"** Deshalb bleiben **Sperre, Zeitsperre und Konto bei jeder Fensterbreite stehen**, während Rolle, Mandanten, Passwortwechsel und letzte Anmeldung weichen — nicht „was passt", sondern „was sucht der Nutzer". **Die zwei Sperren stehen in zwei Spalten und werden nie zu einer** (E14, E20): ein Verwaltungsakt endet nur durch einen zweiten, fünf Fehlversuche enden von selbst nach fünfzehn Minuten; eine gemeinsame Spalte nähme genau die Auskunft weg, für die `lockedUntil` gebaut wurde. **`lastLogin = null` wird ausgeschrieben** — eine leere Zelle sähe aus wie eine fehlende Angabe und beantwortet die häufigste Supportfrage nicht (E17). **Alles Bedienbare steht im Formular unter der Zeile und nicht in den Zellen**, und der Grund ist kein Geschmack: In den Zellen wären drei der fünf Vorgänge am schmalen Fenster gar nicht erreichbar. **Die Vorwarnung aus E19 vergleicht über den Benutzernamen ohne Rücksicht auf die Schreibweise** — `GET /api/auth/me` liefert keine `id`, und ein `===` verfehlte genau den Grenzfall `Admin`/`admin`, für den sie da ist. **Welche Vorgänge am eigenen Konto durchlaufen, ist am Backend abgelesen und nicht geraten**; für die verbotenen gibt es keinen Dialog, sondern die Übersetzung — ein Dialog verspräche, dass es nach dem Bestätigen passiert. **Die drei entwertenden Selbstvorgänge sind bewusst nicht gesperrt, nur aufgefangen**, und der Grund kam beim Lesen des Backends dazu: `pruefeEntwertung` prüft **zuerst** den letzten nutzbaren Administrator, ein selbst gebauter Riegel zeigte also den falschen der beiden Sätze. **Die Mandantenmenge wird vollständig ersetzt**, deshalb Häkchen statt zweier Knöpfe; `SYSTEM` und `WOC` werden **nicht** ausgesiebt, weil die Mandantenauswahl seit Schritt 3 es auch nicht tut. **Fünf benannte Abweichungen**, darunter zwei Befunde: `unbekannte-rolle` gab es im Repository entgegen dem Auftrag nicht, und ein Mandant außerhalb der wählbaren Menge bekommt trotzdem einen Eintrag — bei einer Mengenersetzung löschte eine Auswahl, die ihn nicht darstellen kann, ihn lautlos. **Nicht als erledigt geführt:** Es ist noch kein einziger Vorgang am laufenden System gedrückt worden, und der Grund gehört dazu — jeder schreibt auf die geteilte Testkopie und meldet ein echtes Konto ab |
| [`rollup.md`](rollup.md) | Schritt 10a, erweitert in **Schritt 10b‑1 Teil C** | **`message_rollup`, `message_rollup_tag` und der Job, der sie fortschreibt** — die beiden Tabellen aus `V9`, der Zeitschnitt mit seinem 15‑Minuten‑Nachlauf (gemessen: die Wanderung von `MessageLastUpdate` beträgt höchstens vier Sekunden, M86), die **zwei Uhren in einer Tabellenzeile** (Datenzeit aus der Anwendungsuhr, Protokollzeit aus `systemClock`), Löschen‑und‑Neuschreiben statt Hochzählen, die Monatsscheiben — erzwungen von einer gemessenen Grenze: der Gesamtbereich in **einem** Statement reißt `max_statement_time=10` des Lese‑Pools. Dazu die **zweite benannte Ausnahme von Regel M2** (`RollupLeseRepository` ohne `MandantContext`; die erste ist `@OhneMandantenkontext`) — **nicht** die dritte des Projekts: Die drei Endpunkt-Ausnahmen in [`mandantentrennung.md`](mandantentrennung.md) §3 sind Ausnahmen von **M1** und damit einer anderen Regel. Und die Abnahme: **335.610 Zeilen, `SUM(anzahl)` = 3.341.519, 45,772 s** gegen 51,242 s aus M89. **Kein Endpunkt, keine Oberfläche** — das ist 10b. **Seit dem 27.08.2026 (Schritt 10b‑1 Teil C) sind es zwei materialisierte Ebenen:** `message_rollup_tag` aus `V10` kommt dazu, weil M94 die Zwölf‑Monats‑Ansicht über die Stundenebene mit 2,2 bis 2,5 s gemessen hat und die Kosten dort **linear an der Zahl gelesener Zeilen** hängen — es gibt nichts wegzuoptimieren, nur weniger zu lesen. **Eine eigene Tabelle und keine Ebenen-Spalte:** Sonst stünden ein Tageseimer und seine 24 Stundeneimer nebeneinander, und jede Summe über die Tabelle zählte lautlos doppelt (6.683.038 statt 3.341.519). Beide Ebenen entstehen im **selben Lauf und in derselben Transaktion**, die Tagesebene **abgeleitet aus der Stundenebene** und über **ganze Tage** — ein Tageseimer aus einem Zwei‑Stunden‑Fenster trüge zwei Stunden und behauptete, ein Tag zu sein. Dazu §6a, der **einmalige Rückwärtslauf** (22 Scheiben, 646 Tage, 123.049 Zeilen, 26 s — davon 21 s Drosselung), der bewusst **keine Zeile in `rollup_lauf`** hinterlässt: Er rechnet nichts aus `Message` und dürfte deshalb keinen Wasserstand behaupten. **§9a ist das Tor, und es löst aus:** Die Tagesebene senkt alle acht gemessenen Paare um Faktor 2,7 bis 4,1 und bringt die 30‑Tage‑Ansicht unter 100 ms (45,990 bzw. 62,454 ms) — die Zwölf‑Monats‑Ansicht bleibt beim größten Mandanten aber bei **767,128 ms** (Verlauf) und **908,539 ms** (Verteilung) und damit über der vor der Messung gesetzten Schwelle von 700 ms. **Die Monatsebene ist deshalb eine Vorlage an den Auftraggeber und nicht gebaut worden**; die Zahlen dafür stehen vollständig in §9a |
| `dashboard.md` | Schritt 10 | Kennzahlen, die drei Problemkategorien |
| `process-view.md` | Schritt 10 | Prozessansicht, gruppiert nach kuratiertem Partner |

Diese Tabelle ist eine Vorschau, keine Zusage über Dateinamen. Wer einen Schritt umsetzt, trägt
die tatsächlich entstandenen Dateien hier ein.

### Erhebungen

Einmalig erhobene Fakten über das Quellsystem, die sonst niemand mehr nachvollziehen kann:

| Datei | Entsteht in | Inhalt |
|---|---|---|
| [`messungen-schritt10b.md`](messungen-schritt10b.md) | **vor Schritt 10b** | **M94 bis M98** gegen die Testkopie (27.08.2026), vor dem Bau von Dashboard und Verteilung. **M94** ist die Runde: Was kostet es, Tages- und Monatsansicht zur Lesezeit aus der Stundentabelle zu rechnen? **Die Zwölf-Monats-Ansicht kostet 2,207 s (Verlauf) bzw. 2,491 s (Verteilung) bei `NEXANS`** — das 5,5- bis 6,2-Fache der vorregistrierten 400-ms-Schwelle und das Vier- bis Fünffache des 500-ms-Budgets der ganzen Landingpage. **Nach der vor der Erhebung festgelegten Lesart trägt E‑b damit nicht**; eine materialisierte Tages- oder Monatsebene ist dem Auftraggeber vorzulegen (offener Punkt 55). **Die Runde entscheidet das nicht.** Nebenbefund und für die Abhilfe wichtig: **Der Katalog-Join ist nicht der Treiber** — sein Aufschlag *fällt* mit der Fensterbreite, von ×1,56 bei 48 Stunden auf ×1,13 bei zwölf Monaten; die Kosten hängen linear an der Zahl gelesener Rollupzeilen (5,3 bis 11,5 µs über Faktor 302 an Menge). **M95** erhebt den Anteil belegter Eimer für alle zehn Mandanten und alle drei Paare. Der erwartete Sprung zwischen `NEXANS` und den kleinen Mandanten **tritt nicht ein** — drei Mandanten liegen bei allen drei Paaren bei 100 %, obwohl Faktor 25 an Menge zwischen ihnen liegt —, und `WOC` erfüllt 29 von 30 Tagen mit 3,9 Nachrichten je Tag: **Der Anteil sagt, ob ein Diagramm Punkte hat, nicht ob es etwas zeigt.** Der Vorschlag für die Schwelle braucht deshalb zwei Bedingungen; **die Festlegung gehört in 10b**. `EDITIONLINGERI` trägt bei keinem Paar einen einzigen Eimer — für ihn greift der Leerzustand. **M96** ist die eine Messung, die schreibt: 48 Delta-Läufe über den gebauten `RollupJob` nachgespielt, dann ein Volllauf über dasselbe Fenster. **Die Überzählung ist exakt null**, und der Volllauf trifft die direkte Zahl aus `Message` auf die Einheit (12.004). **Die Null misst aber den Mechanismus und nicht das Phänomen** — auf einem eingefrorenen Bestand kann keine Folge von Delta-Läufen abweichen. Was die Messung wirklich zeigt, ist der **Überlapp von 99,50 %**, den Löschen-und-Neuschreiben schluckt: 23.948 gelesene gegen 12.004 wirkliche Nachrichten. **M97** misst die Listenabfrage mit dem neuen Parameter `ueberfaellig`. **Er ist kein Filter, sondern eine zweite Abfrageform**: Er zieht den Treiber von `MessageLastUpdateIDX` auf `MessageStatusIDX`, macht die Sortierung zum `filesort` und entwertet den Cursor. Bei `NEXANS` kostet er Faktor 3,4, bei `SUTTONS` **spart** er Faktor 158. Dabei ist ein Befund über den **gebauten** Endpunkt aufgefallen: **Die bestehende Liste kippt bei kleinen Mandanten zwischen sieben und dreißig Tagen auf `ProejctIDIDX` und kostet dort das 167-Fache** (1,101 s gegen 6,6 ms) — `messungen-schritt4.md` L4 misst dort nur 24 Stunden. Nebenbei beantwortet: Alle 538 offenen Zeilen sind auch am **geltenden** Anker überfällig, weil jede die Frist 1.800 s trägt und die jüngste 15,5 Stunden davor liegt. **M98** misst die Verteilung **nach** der Kuratierung — und die hat den Katalog verändert: 1.486 statt 1.160 Zeilen, 1.457 statt 770 gepflegt, **505 davon am Morgen der Messrunde**. Sechs Mandanten ohne eine einzige gepflegte Zeile sind seither vollständig gepflegt; **`SUTTONS` nicht** — dort ist die Verteilung zu 100 % *nicht zugeordnet*, und nach der vorregistrierten Lesart ist **der Katalog vor der Abnahme von 10b zu pflegen** (17 Zeilen, offener Punkt 58). Die Konzentration bleibt bei `NEXANS` (57,69 %) und `VOTG` (58,95 %) über 50 %, **bei `IBIS` aber nicht**: dort hält der größte Partner 12,56 %, und die Zeile „Übrige (40)“ wäre der größte Balken des Blocks — die Gestaltung, die `NEXANS` braucht, ist dort genau falsch herum. **Drei der vier Mandanten haben nicht drei Zeilen**, der Block muss fehlende Restzeilen vertragen. Neun Befunde (15–23) passten in keine vorformulierte Zeile, acht offene Punkte (55–62) sind vergeben; teuerstes Statement ist die **Ankerabfrage der Dev-Uhr mit 6,825 s**, und sie ist kein Anwendungscode im Anfragepfad. `@@global.read_only` stand in allen dreizehn Sitzungen auf `1`; der Rollupbereich, den M96 neu geschrieben hat, ist am Ende **Zeichen für Zeichen** wiederhergestellt und über eine Inhaltsprüfsumme nachgewiesen |
| [`messungen-schritt10.md`](messungen-schritt10.md) | **vor Schritt 10a** | **M86 bis M92** gegen die Testkopie (26.08.2026), vor dem Bau des Rollups: **M86** wandert `MessageLastUpdate` nachträglich (höchstens vier Sekunden — daraus folgt das Nachlauffenster), **M87** wie viele Zeilen der Rollup erzeugt (335.610, Verdichtung nur Faktor 9,96), **M88** was der stündliche Delta‑Lauf kostet (88,167 ms für die dichteste Stunde des Bestands), **M89** die Probetabelle und die Leseabfrage des Dashboards, **M90** „Überfällig" live, **M91** die Partnerverteilung, **M92** der Rückwärtslauf. **Nachgetragen am 26.08.2026 mit Schritt 10a**: Der Eintrag fehlte, weil der Auftrag der Messrunde `docs/README.md` unter „Gesperrte Dateien" führte und ihre Abweichung **A12** das ausdrücklich als offen vermerkt hat |
| [`messungen-schritt9.md`](messungen-schritt9.md) | **vor Schritt 9b** | **M74a bis M85** gegen die Testkopie — die Runde selbst (M74a–M79, 20.08.2026) und drei Nachträge: **M80** (20.08.2026, der gebaute Katalog), **M81/M82** (21.08.2026, Schritt **9a** — sie gehören nicht zum Katalog, sondern zur Benutzerverwaltung und sind dort ausgewertet: [`benutzerverwaltung-backend.md`](benutzerverwaltung-backend.md); **diese Zelle hat sie bis zum 21.08.2026 nicht geführt, die Lücke ist vorbestehend und hier nur benannt, nicht rückwirkend gefüllt**) , **M83** (21.08.2026, die Bestandsabfrage zu E14) und **M84** (24.08.2026, derselbe Text **gerendert** — die Messung, die M83 selbst verlangt hat, weil ihr eigener Text von Hand getippt war; identischer Plan, 23,153 ms für `NEXANS` gegen 22,154 ms von Hand, und damit ist Regel L7 am gebauten Statement erfüllt). Vor dem Bau des Prozesskatalogs. **Die Runde ist um eins verschoben**, weil der Auftrag von M72 als letzter Nummer ausging und `M73` am 19.08.2026 vergeben worden war (V1 hat gegriffen). Zwei Vorbedingungen des Auftrags sind selbst widerlegt: **V3** hält „733 Prozesse für `NEXANS`" für ungedeckt — die Zahl ist seit **L12** vom 06.08.2026 an neun Stellen belegt und hier unverändert bestätigt; und die Indexliste, die der Auftrag als „gelesen, nicht erhoben" ausweist, ist **unvollständig**: `Message` trägt **acht** Indizes statt der drei aus `PROJEKTBESCHREIBUNG.md` §3.2, darunter **zwei eigenständige auf `ProcessID`** — womit die Begründung von M74b hinfällig ist (offener Punkt 33, betrifft auch die Verkettung). **M74b** ist der schärfste Befund der Runde: **765 von 1.503 Prozessen (50,90 %) tragen keine einzige Nachricht**, bei `VOTG` 89,74 %, bei `EDITIONLINGERI` 100 % — die Pflegeliste braucht die Kennzeichnung „trägt keine Nachrichten", sonst kuratiert jemand die Hälfte des Katalogs umsonst; nebenbei hält **ein einziger Prozess 44,08 % aller Nachrichten**. **M75** zeigt die Heuristik als Verfahren für **drei** Mandanten und nicht für sieben: der Nummernpräfix liegt bei `NEXANS`, `VOTG`, `SUTTONS` bei **100 %** und bei allen übrigen bei **0 %** — der Gesamtwert 76,51 % hätte genau das verdeckt. **M76** und **M77** bestätigen §4.4, wo es etwas sagt, und finden es lückenhaft, wo es schweigt: die Massenzuordnung nach Projekt erreicht bei `VOTG` nur **157 von 390** Prozessen, weil **226** unter einem einzigen Projekt ohne Partnerbezug hängen, und die Richtung im `SOSName` gilt für `VTG` (90,26 %, **null** Widersprüche) und für `SUTTONS` **nicht** (englisches Vokabular). **M78** hält die Entscheidung „zwei Pflegezustände" geschlossen — der beauftragte Filter `^0+_` fängt jedoch vier reguläre Prozesse mit, darunter einen mit 1.602 Nachrichten, der die Abbruchbedingung fälschlich ausgelöst hätte. **M79 entfällt** auftragsgemäß zugunsten von L12 (4,2 ms beim größten Mandanten, keine Paginierung nötig). **Keine Zeile Anwendungscode, keine Migration, keine `process_catalog`**. — **Nachtrag vom 20.08.2026 (M80), nach dem Bau:** Er schließt die eine Lücke, die M79 ausdrücklich offengelassen hatte — den Join gegen `process_catalog`, den es damals nicht gab. Gemessen mit **gefülltem** Katalog (1.490 Zeilen über den echten Codepfad, danach wieder gelöscht): Die Pflegeliste kostet **8,000 ms** bei `NEXANS` und **0,812 ms** bei `SUTTONS`, der Katalog hängt bei beiden als **`eq_ref` auf `PRIMARY`** am Plan, die Einstiegstabelle bleibt `ProjectMandant`. **Der Join kostet +3,24 ms, also 68 %** — harmlos, aber mehr als „ein `eq_ref` kostet nichts" vermuten lässt. **Die Zahl trägt einen datierten Vorbehalt:** Sie ist **keine Produktionszahl**, sondern ein Verhältnis zweier kleiner Zahlen auf der Testkopie ohne Nebenlast; am **Umfang** liegt es ausdrücklich nicht — der Katalog stand mit 1.490 Zeilen auf voller Größe. **M80‑6 rechnet die Projektion aus `prozess-katalog.md` §3.5 zum ersten Mal nach** und findet sie an zwei Stellen falsch, die einander in der Summe fast aufheben: Regel B liefert **280 statt 281**, weil sechs `IBIS`/`IBISGUS`-Prozesse den Anker gar nicht tragen und fünf `EDITIONLINGERI`-Prozesse ihn sehr wohl. Dieselbe Messung hat einen Fehler im *gebauten* Code gefunden — eine erfundene Wortgrenze, die 26 Prozesse kostete. **M80‑7** liefert die Zahl, die in keinem Dokument stand: Die Heuristik füllt für **1.020 von 1.490** Prozessen eine **Richtung** (68,5 %), bei `NXHBE` für alle 17 und bei `VOTG` für **keinen einzigen** — womit der offene Punkt 4 aus §10 für vier von fünf Mandanten beantwortet ist und **allein `EDITIONLINGERI` offenbleibt**. Teuerstes Statement des Nachtrags: **8,0 ms**, keine Abweichung vom Rahmen **Nachtrag M83 (21.08.2026)** erhebt die Bestandsabfrage zu **E14** (Spalte `traegt_nachrichten`, beim Heuristik-Lauf gefüllt) und entscheidet nichts: Von den zwei Fassungen kostet **A** (`EXISTS` je Prozess) für `NEXANS` **22,154 ms**, **B** (`DISTINCT` über `Message`) **4.797 ms** — **Faktor 216,5**, weil A je **Prozess** zahlt und B je **Nachricht**. Beide Pläne steigen über `ProjectMandant` ein, bei beiden Mandanten; M83‑5 reproduziert M74b auf die Zeile genau (733/516/217 und 17/17/0). **Fünf Befunde passten in keine vorformulierte Zeile**, darunter: der Plan steht auf einer `CARDINALITY` von 18 und schätzt 197.804 Zeilen je Nachschlag, wo 4.528 wahr sind (**Faktor 43,7**), und die beiden `ProcessID`-Indizes sind deckungsgleich — `Message_ProcessFK` wird nie gewählt. Teuerstes Statement: **26,386 s** (Bezugsgrößen nach L9, nie Anwendungscode); **sechs Abweichungen vom Rahmen**, alle methodisch, einzeln benannt. **Nachtrag M93 (26.08.2026)** misst die Lesung der Vorschlagsübernahme (E22) am **gerenderten** Text gegen **vier** Mandanten. Der Plan ist bei allen vier Zeile für Zeile derselbe — Einstieg `ProjectMandant`, `process_catalog` als **`eq_ref` auf `PRIMARY`**, kein `STRAIGHT_JOIN` —, und die teuerste Zahl der Runde sind **3,562 ms**, also 0,036 % der Grenze des Lese-Pools. **Ihr Hauptbefund steht aber nicht in der Laufzeit:** Die vorregistrierten Erwartungen des Auftrags (`NEXANS` 509, `IBISGUS` 88) sind **nicht eingetreten**, und der Grund ist der, den der Auftrag selbst als Meldefall benennt — es ist kuratiert worden. `NEXANS` steht auf **733 gepflegten Zeilen und null offenen** (ein Lauf hilft dagegen nicht, E13), `IBISGUS` hat **gar keine** Katalogzeile. Belegt ist die Bedingung stattdessen an `VOTG` (**377** aus Regel A gegen die 378 aus §3.5) und an `IBIS`, wo **zwei bereits gepflegte `REGEL_B`-Zeilen korrekt herausfallen** — 169 von 171, die schärfste Einzelkontrolle der Runde. Zwei Befunde standen in keiner vorformulierten Zeile: `Using temporary` ist **neu** gegenüber M80 (Sortierung nach `ProcessID` bei Einstieg über `ProjectMandant`, folgenlos bei höchstens 733 Zeilen), und **M93-6 hält einen Fehler an der eigenen Arbeit fest** — der erste Entwurf des Isolationstests legte seine Zeile per Upsert an und hat dadurch eine `VOTG`-Katalogzeile gelöscht; die Aufräumung über das Präfix `it-` unterscheidet nicht zwischen „angelegt" und „zuletzt angefasst". Behoben an der Ursache, ein Knopfdruck auf den Lauf stellt die Zeile wieder her |
| [`messungen-schritt8.md`](messungen-schritt8.md) | **vor Schritt 8** | **M52 bis M59**, **M64a** und **E8** gegen die Testkopie und gegen den Filestore (14.08.2026), vor dem Bau des Rohdaten-Downloads. **Teil A ist vollständig gefahren, Teil B ist an seinem ersten Lauf stehen geblieben** — und das ist der tragende Befund: **M59** findet hinter dem `ServiceConnectString` einen **SOAP-Empfänger** (`/WebApplication/FileStoreSoapReceiver`), keine Dateiadresse. Er ist erreichbar (`200`, `text/xml`, 185 Byte, **keine Authentifizierung**, 6,08 ms), liefert aber **keine Schnittstellenbeschreibung** (`?wsdl` gibt dieselben 185 Byte), antwortet auf **vierzehn** geprüfte Abfrageparameter mit einer **leeren `FileList`** und auf die Kennung im Pfad mit `404`. Der nächste Schritt wäre ein `POST` mit geratenem Rumpf — **nicht gegangen**, weil ein *Receiver* die Stelle ist, an der Dateien abgelegt werden, und die Runde ausdrücklich rein lesend ist. **V3 und V5 bleiben damit offen, und M60, M61, M63, M65, M66, M67 sind nicht gefahren** (ihre Nummern bleiben vergeben). **M52** korrigiert nebenbei eine Zahl, die alle drei verbindlichen Dateien tragen: Es gibt **elf** Filestores (`FILESTOREPROD00`–`10`) und nicht den einen `FILESTOREPROD09` des Beispiels. **M53** beantwortet V7 mit einer dritten, nicht vorgesehenen Lesart: Die Ablage hängt **nicht am Mandanten, sondern am Zeitpunkt** — je Zeitraum sind **zwei** gleichzeitig aktiv, und **jeder** Mandant verweist auf beide; `nicht_aufloesbar` ist über alle drei Fenster **null**. **E8** (ergänzend, nicht im Auftrag, entstanden weil M53 die Rotation fand) legt die Umschaltung taggenau auf den **23.07.2025** — dieser eine Tag trägt alle vier Kennungen — und findet dabei den Befund, der die Aufgabenstellung selbst trifft: **Der Bestand hat keine 22 gefüllten Monate**, sondern 15 volle (2024-10 bis 2025-12), **fünf leere** (2026-01 bis 2026-05) und zwei Bruchstücke (4.848 im Juni, 285 am 08.07.2026). **Drei der fünf Zeitscheiben von M66 sind damit hinfällig, bevor M66 startet.** **M54** entlastet zweifach: Die gestaltbasierte Gegenprobe ist in **beiden** Fenstern leer (die Artefaktmenge darf über die zwei Namensendungen definiert werden), und `mit_pipe = zeilen` in jeder Zeile bei durchgängig 52 Zeichen (keine Prüfung je Wert nötig) — 32 Namen in Fenster A, 34 in B, davon zwei Familien **ohne** Protokoll (`Message`, `DataWarehouse`). **M55** ist die Messung mit den meisten Überraschungen: Das Maximum liegt bei **genau 15** und damit in **keiner** der beiden vorformulierten Zeilen (weder „≤ 8" noch „> 15"); der deaktivierte Knopf hat die gemessene Häufigkeit **null** (Minimum 3 über 220.579 Nachrichten); und `nachrichten_mit_protokoll = nachrichten` bei **allen acht** Mandanten — es gibt keine leere Protokollansicht. **M56** belegt, dass eine Vorabgröße **nicht** verlässlich verfügbar ist (69,6 % bzw. 57,2 % Abdeckung) und die Kappung der **Normalfall** ist (81 Dateien über 10 MiB in einem Monat, größte **118,2 MiB**, eine mit Größe 0) — die **Einheit** dieser Spalte bleibt allerdings unbelegt, weil M60 sie hätte prüfen sollen. Dazu ein Befund über die Messung selbst: `OriginalFilename` trägt **praktisch keine Dateiendung** (0,004 % echte Endungen in Fenster B, sonst Ziffernfolgen), und der **im Auftrag vorgegebene G1-Filter `^[a-z0-9]{1,5}$` hätte 7.476 verschiedene Zahlenfragmente aus echten Dateinamen unmaskiert in das Messdokument geschrieben** — gefahren ist eine verschärfte Fassung, die einen Buchstaben an erster Stelle verlangt. **M57**: `ohne_schrittzeile = 0`, aber 56 % ohne Schrittnamen — zwei Drittel davon auf dem Metadaten-Schritt, der Rest ist die **bekannte** `FTPSender`-Lücke aus M15 (3.985, auf die Zeile reproduziert); alle Reader-Familien sitzen auf Schritt 0. **M58** misst den Zugriffsweg mit **0,796 ms** (Artefaktliste), **0,351 ms** (Auflösung) und **0,440 ms** (Existenznachweis), je beste von fünf, dazu die Gegenprobe `Impossible WHERE noticed after reading const tables` — und zeigt `Using where` statt des erwarteten `Using index`, **ohne** dass die daran geknüpfte Folgerung greift: Der Endpunkt **muss** den Wert lesen, und unter InnoDB *ist* der Primärschlüssel die Zeile. **M64a** liefert die Entwarnung, auf die es fachlich ankam — Fehlernachrichten tragen ein Protokoll, **11 von 11** in Fenster B und **170 von 170** im erreichbaren Zeitraum —, findet dabei aber einen Widerspruch zu [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) §4.1: **`ERROR_DUPLICATE` ist 3.248 statt 659** (Faktor 4,93), während die beiden anderen Zahlen dort exakt reproduzieren. **Nichts daran ist geändert worden** — wie bei M33‑0 gehört die Korrektur der verbindlichen Datei ins Sparring. Je mit Statement, `EXPLAIN` und Laufzeit; die Deutungen standen **vor** der Erhebung fest, und die **neun** Stellen, an denen der Befund in keine vorformulierte Zeile passt, sind einzeln benannt und gezählt. **Sieben Abweichungen vom Rahmen** sind ausgewiesen, darunter zwei, die jede künftige Runde betreffen: `profiling_history_size` steht auf **15** und nicht auf 100 (der erste M58-Lauf verlor dadurch die Laufzeiten seiner ersten 23 Statements), und `--skip-ssl` kennt der Workbench-Client weiterhin nicht. Kein Statement hat die 60-Sekunden-Grenze gerissen; das teuerste liegt bei **96,8 %** davon (M54 (a) Fenster B, 58,069 s). Vier Kontrollen gehen auf (6.249 und 214.330 aus M17, NEXANS' 180.251, die Verweissummen aus M53 gegen M55 für alle acht Mandanten). Das Arbeitsverzeichnis von Teil B ist gelöscht; **kein Dateiinhalt ist gelesen worden, weil keiner abrufbar war**. **Abschnitt QT, am selben Tag nachgereicht und als einziger *gelesen* statt *gemessen*:** Der Auftraggeber hat den Quelltext der Abrufmethode des Altwerkzeugs beigesteuert, und er ändert drei Dinge, die keine Messung dieser Runde gefunden hätte. **QT2 — die Datei kommt als ZIP**, womit der in M60 vorgesehene Vergleich `Content-Length` gegen `FileReader.FileProperty.Size` nicht trägt und der Proxy aus Schritt 8 einen **Entpackschritt** braucht, den der Implementierungsplan nicht kennt. **QT4 und QT6 — das Altsystem beschneidet Protokolle nicht nach der Regel, die der Auftrag als einzig sichere vorschreibt:** Es hängt **alle** Abschnitte zwischen den Marken aneinander (die geplante Regel nimmt nur den ersten) und liefert bei **fehlender Endmarke alles ab der Startmarke** statt nichts — also genau im Fall des abgebrochenen Laufs, um den M64 gebaut war, das Gegenteil von *fail closed*. Die geplante Regel ist damit **strenger als die heutige**, und das ist eine Verhaltensänderung und keine Nachbildung. **QT7 — das Altsystem maskiert zwei absolute Pfadpräfixe *innerhalb* der Marken**, was die zweite vorregistrierte Zeile von M65 vorwegnimmt und in keiner Datei dieses Projekts bisher erwähnt war. Dazu **QT3** (fest verdrahtete **ISO-8859-1**-Dekodierung, die UTF-8-Festlegung steht auskommentiert daneben), **QT5** (Markenerkennung mit `contains` über die ganze Zeile, ohne Normalisierung — die Beeinflussbarkeit ist damit gelesen statt abgeleitet), **QT8** (drei Stellen, die das neue Werkzeug nicht nachbauen darf: Rolle aus einem Anfrageparameter, ungeprüfter Dateiname in `Content-Disposition`, Disposition nur auf Anforderung) und **QT9** (auch `ADMIN` bekommt heute **nicht** die Rohdatei — beide Zweige laufen über dieselbe Dekodierung und `LF`-Vereinheitlichung, auch bei Nutzdaten; der geplante Bytestrom-Proxy ist deshalb eine Verbesserung und keine Nachbildung, und ein Abnahmevergleich mit der alten Ausgabe wird Unterschiede zeigen). **QT1 beantwortet V3 vollständig**, nachdem am selben Tag auch der Quelltext des Clients selbst nachgereicht wurde (`alterCode/OverlordFilestoreClient`): **SOAP 1.1 mit Anhängen (SwA)**, Kopfzeile abgehängt, Anfrage als `m:FileList` mit **unqualifizierten** `File`-Kindern und den Attributen `Counter`, `ID` und `Action` — genau das Element, das M59 als *leere* Antwort auf ein blankes `GET` gemessen hatte. Die Datei kommt als **Anhang** zurück, zugeordnet über `Content-ID` gleich `Counter`; die Antwort trägt je Datei zusätzlich ein `Response`-Attribut. Entscheidend für die Sorge, die den Abbruch ausgelöst hatte: **nur `CREATE` hängt Nutzdaten an**, `RETRIEVE` und `DELETE` haben leere Zweige — eine `RETRIEVE`-Anfrage kann konstruktionsbedingt nichts ablegen. **Offen ist damit keine Information mehr, sondern eine Freigabe:** Der Aufruf gegen den laufenden Filestore ist zweimal an der Berechtigungsprüfung der Entwicklungsumgebung gescheitert, die `curl` mit `--data-binary` gegen einen internen Rechner abweist. Danach laufen M60, M61, M63, M65, M66 und M67 ohne weitere Vorbedingung. Entschieden ist dagegen die Ablagenfrage — **zusätzlich `FILESTOREPROD09`**, beide erreichbar gemessen, womit der Zeitraum ab dem 23.07.2025 vollständig abgedeckt ist und alle **170** Fehlernachrichten statt nur 73 in den erreichbaren Bereich fallen. — **Diese Beschreibung ist der Stand vom 14.08.2026 und überholt**; die Datei trägt inzwischen **M52 bis M73** samt Abschnitt Q. **Nachtrag 19.08.2026 — M73:** `Message.Payload.GUID` ist **nicht** die eingegangene Datei, sondern der Verweis der Nutzdatenzeile mit dem höchsten `MessageActionID` derselben Nachricht, in 6.249 von 6.249 und 214.330 von 214.330 Nachrichten ohne Gegenfall. **Nicht gemessen** ist, ob das die zuletzt *erzeugte* Datei ist — „höchster `MessageActionID`" ist die Schrittnummer, nicht die Uhr; hier stand bis zum 19.08.2026 „**die zuletzt erzeugte Datei**", und das war die Deutung und nicht die Messung. Die Beschriftung *Eingegangene Datei* in `rohdaten-frontend.md` §2 und `rohdaten.md` §5 traf damit in 0,016 % bzw. 0,015 % der Nachrichten zu. **Korrigiert am 19.08.2026** in einer eigenen Runde: Das Artefakt ist aus der Liste und das Feld `eingang` aus `ArtefaktlisteResponse` entfallen; die alten Aussagen stehen wörtlich mit datierten Korrekturkästen daneben (`rohdaten.md`, `rohdaten-frontend.md`, `rohdaten-backend.md`, `PROJEKTBESCHREIBUNG.md`) |
| [`messungen-schritt8-auftrag.md`](messungen-schritt8-auftrag.md) | **vor Schritt 8** | Die **Aufgabenstellung** zu der Messrunde oben, **Fassung 3** — nachgetragen am 19.08.2026, weil sie als einzige Datei in `docs/` in diesem Verzeichnis fehlte. **Keine Erhebung, sondern ihr Auftrag**, und deshalb steht sie hier neben der Messdatei und nicht unter „Features": welche Messung freigegeben war und welche gesperrt (`CREATE`, `APPEND`, `DELETE` durchgängig, jeder Rückfall auf eine andere Ablage, jede Wiederholung eines Fehlversuchs mit abgewandelten Werten), die **vorregistrierten Lesarten** zu jeder Messung, die drei Zeitfenster, der G1-Rahmen für das, was gezeigt werden darf, und die **fünf Ergänzungen vom 17.08.2026**, mit denen der Alt-Client `FilestoreClient` schrittweise freigegeben wurde — zuerst gegen einen Lauscher auf `127.0.0.1`, dann gegen einen einzigen benannten Verweis, dann über die Stichproben. Sie ist der Beleg dafür, dass die Deutungen **vor** den Zahlen festgestanden haben, und ohne sie ist der Satz „die Deutungen standen vor der Erhebung fest" in der Messdatei unbelegt. Ihr Abschnitt „Was diese Runde nicht tut" enthält außerdem die Stelle, an der der Widerspruch um die Anzeige zum ersten Mal benannt und aufgeschoben wurde: „**Das ist eine Dokumentationsfrage, keine Messfrage — und sie ist datiert zu entscheiden.**" Entschieden ist er am 19.08.2026 in [`IMPLEMENTIERUNGSPLAN_MVP.md`](IMPLEMENTIERUNGSPLAN_MVP.md) und [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) |
| [`message-status.md`](message-status.md) | **Schritt 2** (vorgezogen) | Ergebnis von `SELECT DISTINCT MessageStatus` — widerlegt Annahme A6. Ersetzt das ursprünglich für Schritt 3 geplante `message-status-werte.md` |
| [`messungen-schritt7.md`](messungen-schritt7.md) | **vor Schritt 7** *(M46 bis M48 währenddessen, **M49 bis M51 danach**)* | **M32 bis M51** sowie **E6** und **E7** gegen die Testkopie (Hauptrunde 11.08.2026, zwei Nachträge 12.08.2026, dritter bis **siebter** Nachtrag 13.08.2026, **achter und neunter** 14.08.2026), vor dem Bau der BAM-Suche — die erste Runde, die `MessageBAM` **über den Wert** statt über die `MessageID` anfasst, und zugleich die Erhebung, die Regel L8 für diese Tabelle nachholt. **M32** findet einen **Vollindex** über `varchar(70)` (kein Präfixindex, anders als bei `MessageProperty`) und einen zusammengesetzten Index, der den **Typ voranstellt** und für die typlose Suche deshalb unbrauchbar ist. **M33** ist die tragende Zahl: Median **1** Treffer je Wert, 99. Perzentil **75**, Maximum **234.159** — die Verteilung ist zweigipflig (76,74 % der Werte haben genau einen Treffer, 29 Werte tragen 8,53 % aller Zeilen), womit die Trefferzahl und **nicht die Zeichenlänge** das richtige Schutzkriterium ist und die Rückfalloption aus **Annahme A9 nicht gebraucht** wird. Dabei fällt der folgenreichste Nebenbefund an: **`MessageBAM` hat 15.406.350 Zeilen, nicht die in [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) §8 und [`datenmodell.md`](datenmodell.md) §3 als „gemessen" geführten 10.859.666** (+41,9 %; die alte Zahl ist die `information_schema`-Schätzung). ~~**Nichts daran ist geändert worden** — wie bei M31‑3 gehört die Korrektur der verbindlichen Datei ins Sparring.~~ ✔ **Erledigt am 12.08.2026.** Der Satz bleibt stehen, weil er den Stand vom 11.08.2026 richtig wiedergibt; die Korrektur ist am Folgetag erfolgt und steht in [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) (§3.2, §8 mit neuer Spalte `Herkunft`, A7, A8 und eigenem Korrekturkasten), [`datenmodell.md`](datenmodell.md) (§3, §8, eigener Kasten) und [`annahmen-korrekturen.md`](annahmen-korrekturen.md) (neuer Abschnitt „Erhebung 11./12.08.2026"), dazu die Regelliste zu **L8** in [`../DEVELOPMENT_GUIDELINES.md`](../DEVELOPMENT_GUIDELINES.md) §4.4. **M34** misst den Einstieg über den Wert: exakt **0,8 ms** im Normalfall und **10,6 s** im schlimmsten — der einzige gemessene Fall, der die 10-Sekunden-Grenze des Lese-Pools reißt; Präfix kostet bei **gleicher Trefferzahl** nur 3 bis 27 % mehr, `enthält` dagegen **40,2 s für einen Wert mit einem Treffer** und ist damit mit Zahl statt mit Vermutung ausgeschlossen. `STRAIGHT_JOIN` ändert bei exakt und Präfix nichts — die L15-Falle sitzt hier bei `enthält` (Faktor 4,3). **E6** (ergänzt) benennt die eigentliche Kostengröße: Ein **verkürztes** Präfix hebt dieselbe Suche von 1 auf 155.871 Treffer und von 1 ms auf 3,4 s — die Wahl zwischen exakt und Präfix ist damit keine Leistungs-, sondern eine Trefferzahlfrage, und eine feste Mindestlänge über alle Typen ist der falsche Hebel. **M35** entscheidet über Regel L1: Das Pflicht-Zeitfenster **wirkt** (24 h → 90,5 ms, ein Monat → 1,65 s, ein Jahr → 8,66 s, ohne → 10,75 s), aber nur **eng** — die vorformulierte Lesart „dann eine weite Vorgabe" ist widerlegt, und beim schlimmsten Wert liefert das Standardfenster 279 von 234.159 Treffern. **M36**: die Typeinschränkung beschleunigt **nicht** (+1,5 %) und ist reine Ergebnisverfeinerung. **M37** widerlegt die Vermutung hinter den identischen Anzahlen aus M28‑2: `9032`/`9033` und `9022`/`9023` teilen **null** Werte — es ist `9028`/`9029` mit 15.790; Mehrfachtypen gibt es bei 4,17 % der Paare. **M38** beantwortet, ob exakte Suche benutzbar ist: **sechs Typen tragen auf 100 % ihrer Werte eine führende Null**, die kuratierte Lieferschein-Nr. 9006 auf 33,88 %, und **9018 — der tragende Suchtyp von `NEXANS` — trägt auf 25,88 % Leerzeichen am Rand**. **M39** erweitert M28‑2 auf **alle acht** Mandanten mit Nachrichten und findet die tragende Rolle je Mandant auf **vier verschiedenen** Rollen (Wurzel bei `NEXANS`/`IBIS`, **Kind** zu 100 % bei `SUTTONS` und `ZAST`, Merge-Ergebnis bei `NEXANS`, ohne Kette bei `WOC`); nebenbei ist die Null der Merge-Eingänge aus M26 von einer Tagesstichprobe auf einen Monat und alle Mandanten gehoben (**38.628 von 38.628 ohne einen einzigen BAM-Wert**). **M40** entscheidet die Frage nach `MessageBAMMandant`: Sie ist **Sichtbarkeit, nicht Bestand** — `WOC` trägt 2.067 BAM-Zeilen unter Typ 9014, den seine Konfiguration nicht kennt, während `SYSTEM` bei gleicher Lage null trägt und `EDITIONLINGERI` gar keine Nachricht hat. **M41** bemisst den Deckel des BAM-Blocks: 99. Perzentil **186** (Wurzel) und **1.345** (Merge-Ergebnis), Maximum **9.296** auf einer Nachricht, bis zu **18** verschiedene Typen — und der Mandant, der die Deckelung erzwingt, ist der **zweitkleinste**: `ZAST` trägt 441 Werte je Nachricht. Je mit Statement, `EXPLAIN` und Laufzeit; die Deutungen standen **vor** der Messung fest, und die **achtzehn** Stellen, an denen der Befund in keine vorformulierte Zeile passt, sind einzeln benannt und gezählt. Ausgewiesen sind außerdem **drei Abweichungen vom Rahmen** — zwei Kaltläufe über der 60-Sekunden-Grenze (62,8 s und 198,6 s, warm um Faktor 6,0 bzw. 11,3 billiger) und eine nach 679 s abgebrochene Auswahlabfrage. **Nachtrag vom 12.08.2026 (M42, M43)** — zwei Zahlen, die die Sparring-Entscheidungen desselben Tages offen gelassen hatten; die Hauptrunde bleibt unangetastet, die Testkopie ist byteidentisch, und der Vergleichsanker reproduziert M34 auf 0,3 % genau. **M42** misst die **Verundung mehrerer Suchbegriffe**: Jeder weitere Begriff kostet **0,094 ms** (0,666 ms bei zwei, 0,947 ms bei fünf), und der Optimierer steigt **in jeder Konstellation und in beiden Reihenfolgen** über den seltensten Begriff ein — die Verundung mit dem schlimmsten Wert der Runde kostet **0,672 ms statt 10.646 ms**, Faktor 15.843. Damit ist eine Deckelung bei zwei Begriffen fachlich nicht nötig. Drei Befunde stehen dagegen: **`STRAIGHT_JOIN` erzeugt hier die Reihenfolgeabhängigkeit, gegen die er vorgesehen war** (Faktor 219 bis 1.094) und gehört deshalb **nicht** in die Verundung; **zwei Begriffe ähnlicher Häufigkeit belasten**, statt zu entlasten (K3a 27,3 gegen 11,1 ms allein), weil gemeinsam gesetzte Typen einander nichts wegschneiden (M37); und auf einer Nachricht mit **2.499 BAM-Werten** kostet der zweite Begriff den **Faktor 112** (926,7 gegen 8,3 ms), weil der Plan dort von `MessageBAM_BAMValueOnly` (`rows` 1) auf `PRIMARY` (`rows` 8) kippt und die 8 um Faktor 312 zu klein ist. Die Bauform ohne Selbstjoin (`IN` + `HAVING COUNT(DISTINCT …)`) ist **gegenläufig**, nicht schlechter: 954× langsamer bei fünf Begriffen mit einem häufigen, **87× schneller** auf fetten Nachrichten. Bei `IBIS` (Regel L7) gibt es den Bösfall nicht — größte globale Trefferzahl **216**. **M43** liefert das **Maß der Normalisierung**: Die **Sollänge** existiert bei den sechs Typen mit durchgängig führender Null (genau **eine** Länge über den Bestand, und `LPAD` trifft in allen acht geprüften Fällen den Originalwert), **nicht** aber bei den tragenden Suchtypen — 9003 streut über **33** Längen, 9018 über 16. Typ **2001** widerlegt die Ableitung aus einem Monat **innerhalb derselben Erhebung**: 100 % führende Null über Fenster B, **67,21 %** über den Bestand. Der Bestand selbst ist uneinheitlich — bei **9017** tragen **52,98 %** der numerischen Werte eine zweite Schreibweise derselben Nummer, mit bis zu **fünf** Varianten; das heilt keine Normalisierung. Und die 25,88 % Randleerzeichen bei 9018 aus M38 zerfallen in **24,83 % folgende** (unter der **PAD SPACE**-Kollation beim `=`-Vergleich folgenlos, belegt an echten Werten mit 12 gegen 12 Treffern) und **1,05 % führende** (nicht folgenlos: 23 gegen 3) — was Frage 4 der Hauptrunde erheblich entlastet, ohne sie zu erledigen. **Zweiter Nachtrag vom 12.08.2026 (M44)** — die Messung, die aus der Korrektur einer Zahl die Korrektur einer **Überschrift** gemacht hat: `MessageProperty` hat **75.571.462** Zeilen statt der als „gemessen" geführten 46.964.279, die Schätzung lag **60,9 % zu niedrig** — die größte Abweichung, die dieses Projekt gefunden hat, und sie stand ausgerechnet bei der Tabelle, für die M14 den Zähllauf als „den Aufwand nicht wert" abgelehnt hatte. Die Zählung kostet **199,380 s** kalt gegen **20,633 s** warm (Faktor 9,66); der Optimierer wählt `PRIMARY` und liest damit die vollen 15,09 GB Nutzdaten, und sein `rows` trägt dabei selbst die falsche Schätzung. Die 60-Sekunden-Grenze war dafür **vorab ausgesetzt** und durch `SET max_statement_time = 900` ersetzt (Regel L9) — gebraucht wurden 22,2 % davon. Der folgenreichste Nebenbefund ist nicht die Zahl, sondern was sie **auflöst**: Die dokumentierten „rund vierzehn Zeilen je Nachricht" gegen die in M17 gemessenen 22,57 galten seit dem 07.08.2026 als Effekt **verschiedener Nenner** — gerechnet aus der gezählten Zahl sind es **22,62**, und die Erklärung war fünf Tage lang eine plausible Zudeckung. Nebenbei fallen zwei weitere geschätzte Zeilen des Mengengerüsts an (`Process` **1.503** statt 1.490, `Project` **140** statt 142, beide seit dem 01.08.2026 gezählt und nie nachgezogen), womit beziffert ist, wie weit das Muster reicht: **von sieben Zeilen war eine gezählt**. **Dritter Nachtrag vom 13.08.2026 (M45, E7)** — die kleinste Runde und die einzige, deren Hauptbefund aus einer **Stammdatentabelle** von 62 Zeilen stammt. **M45** beantwortet den ersten offenen Punkt von [`bam-werte.md`](bam-werte.md): Die Typbeschreibungen tragen **genau drei** Endungen (`_L_SAP` 20, `_K_SAP` 13, `_FORS` 7; **22 von 62 tragen keine**), und ohne sie fallen **62 Beschreibungen auf 57** — die Kürzung wäre **nicht** verlustfrei. Zwei Befunde, die der Auftrag nicht vorgesehen hatte: Zwei der drei Kollisionen treffen die **endungslosen** Grundtypen 0 (`Bestellnummer`) und 3 (`Rechnungsnummer`), sind also durch keine Ausnahme an den 9xxx-Typen zu heilen; und die Kollision **trifft tatsächlich zusammen** — `Abladestelle_L_SAP` und `Abladestelle_K_SAP` stehen über Fenster B auf **3.405 Nachrichten gemeinsam** (11,3 % aller mit einer `Abladestelle`), `NEXANS` konfiguriert überdies alle drei zugleich. Dass die Endung *unterscheidet*, ist damit gemessen; *was* sie bedeutet, bleibt eine benannte **Vermutung** — jede der drei Kollisionen paart verschiedene Endungen, keine dieselbe, aber bestätigen kann das nur das Altsystem. **E7** stand nicht im Auftrag und ist entstanden, weil die Nacharbeit eine Bauform mit einem ungemessenen Satz begründete: **2,97 %** der BAM-Werte tragen ein Leerzeichen **innen** (27.792 von 936.529, 16 Typen, angeführt von 9016, 9003 und **9018**) — womit ein Leerzeichen als Trennzeichen ausscheidet und die Marke als Wortgrenze belegt statt behauptet ist. Teuerstes Statement des Nachtrags: **4,650 s**, keine Abweichung vom Rahmen. **Vierter Nachtrag vom 13.08.2026 (M46)** — die erste Messung dieser Runde, die **gebaut** wird: Aus ihr entsteht die Sollängen-Kuratierung (`V5__bam_sollaenge.sql`, [`bam-sollaengen.md`](bam-sollaengen.md)). Sie holt nach, was M43 offen ließ — die Längendominanz über den **Bestand** für **alle** Typen statt nur für sechs, und die ungemessene Frage, ob die Sollänge je **Mandant** dieselbe ist. **Sie ist es nicht**, und der Befund hängt nicht an einem Ausreißer: Typ 2000 dominiert bei `SUTTONS` mit Länge **6** (97,33 %) und bei `VOTG` mit Länge **7** (84,06 %); Typ 9014 kommt über den Bestand auf 58,45 %, bei **`WOC`** aber auf **95,21 %** bei 79,15 % führender Null — ein typweiter Schnitt hätte ausgerechnet den Eintrag verworfen, der für diesen Mandanten trägt; und Typ 2001 hat bei `VOTG` 100 % Dominanz und **null** Werte mit führender Null. Die Kuratierung ist deshalb nach `(mandant_id, bam_typ)` geschlüsselt. Nebenbefunde: Der **Bestand kennt 55 Typen**, Fenster B kannte 46 — drei der neun zusätzlichen tragen auf 100 % ihrer Werte eine führende Null und wären einer Kuratierung aus dem Monat entgangen. Über den Bestand tragen **genau zwei** Typen ein **führendes** Leerzeichen (9018 mit 31.193 von 2.311.236, 9020 mit **einer** Zeile), und **keinen einzigen leeren Wert** — gemessen über `LIKE ' %'` und nicht über `LEFT(v,1) = ' '`, weil unter PAD SPACE `LEFT('',1) = ' '` **wahr** ist und der Leerstring sonst mitgezählt hätte (der Beleg steht daneben). Dazu eine Messfalle, die zweimal gemessen werden musste: `ROUND(100 * AVG(v LIKE ' %'), 4)` liefert für 9018 **1,3500 %** statt der richtigen **1,349624 %**, weil `@@div_precision_increment = 4` das `AVG` kürzt, **bevor** mit 100 multipliziert wird — die Zählwerte waren in beiden Fassungen richtig, die Anteile nicht, und für M46‑1/M46‑2 ist die Falle folgenlos, weil dort durchgängig `SUM(…)/SUM(…)` steht. **Fünfte Abweichung vom Rahmen:** Die erste Fassung von M46‑3 ist bei 60 s **serverseitig abgebrochen** worden — der erste Fall der Runde, in dem die gesetzte Grenze wirklich gegriffen hat; Auslöser waren nicht teure Zugriffspfade, sondern ein `LEFT JOIN` auf 62 Stammdatenzeilen, eine Gruppierung über deren Beschreibung und ein `TRIM` je Zeile. Die schlanke Fassung kostet **9,558 s**. M46‑2 lief in **fünf Stapeln** zwischen 28,3 s und 43,6 s, weil ein Lauf über alle 36 Typen die Grenze rechnerisch reißt und die Grenze **nicht** ausgesetzt wird. **Fünfter Nachtrag vom 13.08.2026 (M47)** — die erste Messung dieser Runde gegen ein **gebautes** Statement statt gegen eine Nachbildung: Der gemessene Text ist aus dem Repository gegen eine jOOQ-Attrappe **gerendert** und unterscheidet sich an keiner Stelle vom ausgelieferten; die Prüfwerte stehen als Sitzungsvariablen darin (Regel G1). Sie misst den Endpunkt aus [`bam-suche.md`](bam-suche.md) und schließt zwei ausdrücklich offene Zahlen. **Erstens: Das 30-Tage-Fenster fängt beide Bösfälle der Verundung** — K3b fällt von 5.275 auf **882 ms**, K5c von 926,7 auf **6,9 ms** (Faktor 134); bei K5c sagt der `EXPLAIN`, warum: Mit Fenster kippt der zweite Begriff **nicht** mehr auf den `PRIMARY`-Zugriff mit `rows` 8, den M42 dort als um Faktor 312 zu klein entlarvt hatte. Eine eigene Grenze für die Verundung wird damit nicht gebraucht (offene Frage 10). **Zweitens: Eine Suchvariante kostet rund 0,1 ms** — womit auch beziffert ist, was die beiden nachweislich wirkungslosen Kuratierungseinträge kosten, und die Frage danach keine Leistungsfrage mehr ist. Die Zahlen des Endpunkts: Normalfall **1,1 ms**, schlimmster Wert **1,656 s** über 30 Tage und **8,940 s** über ein Jahr (**89 %** der Zeitgrenze des Lese-Pools — genau der Fall, für den `suche-abgebrochen` existiert), fünf Begriffe **1,622 ms**, `IBIS` 1,185 ms (Regel L7). **Regel L15 ist belegt und nicht angenommen:** Der Optimierer führt in *jeder* Konstellation die Tabelle mit dem selteneren Begriff an — auch dann, wenn dieser im Statement an **fünfter** Stelle steht, dann führt der Plan `b5` an; deshalb steht kein `STRAIGHT_JOIN` darin. Und die offene Frage nach der `IN`-Liste ist beantwortet: Sie hebt die Zugriffsart von `ref` auf `range`, **Index und Einstiegstabelle bleiben**. Der Befund, der in keiner vorformulierten Zeile stand, ist zugleich der teuerste: **Die Anzeigespalten neben statt über der Deckelung kosten Faktor 1,48** (2.443 gegen 1.656 ms) — **bei identisch gutem `EXPLAIN`**, in beiden Fassungen jeder Zugriff `eq_ref` auf `PRIMARY`. Es ist derselbe Fehler wie in Teil 1 (dort Faktor 18,4) und aus demselben Grund: Der Plan sagt nicht, *wie oft* eine Zeile angefasst wird. Nebenbei korrigiert M47 zwei Zahlen des Auftrags: `NEXANS` hat **acht** Sollängen auf zehn Zeilen und nicht elf (die drei *verschiedenen* stimmen), und die zweite Abfrage wählt `MessageBAM_MessageFK` statt des Primärschlüssel-Präfixes — derselbe Zugriffstyp, dasselbe `Using index`. **Keine Abweichung vom Rahmen**, kein Statement über der 60-Sekunden-Grenze. **Sechster Nachtrag vom 13.08.2026 (M48)** — die kleinste Messung dieser Runde und die einzige, die ausschließlich **Stammdaten** anfasst: `GET /api/bam/typen` aus Teil 3 kostet **0,534 ms** beim größten Mandanten (`NEXANS`, 40 konfigurierte Typen) und **0,410 ms** bei einem ohne jede Konfiguration (`WOC`). Der Zugriff ist `ref` über `MandantIDSortIndexBAMTYpeIDX` mit **`Using index`**, die Beschriftung hängt als `eq_ref` darüber — und weil der Index `(MandantID, MessageBAMTypeSortIndex, MessageBAMType)` **Filter und beide Sortierschlüssel** trägt, entsteht **kein `filesort`**. Beide Tabellen zusammen zählen **131 Zeilen**. Der Befund, der in keiner vorformulierten Zeile stand: **Der Sortierindex ist nicht eindeutig** — `VOTG` hat 12 Typen auf 11 Indizes, weil 2002 (`InvoiceNumber VTG`) und 2011 (`InvoiceNumber Vendor`) beide den Index 2002 tragen; ohne einen zweiten Sortierschlüssel entschiede dort die Reihenfolge der Speicherung. Nebenbei bestätigt sie M40 aus der Gegenrichtung: `EDITIONLINGERI`, `SYSTEM` und `WOC` kommen in `MessageBAMMandant` **gar nicht vor**. **Siebter Nachtrag vom 13.08.2026 (M49)** — die einzige Runde dieser Datei, die **nichts baut und nichts entscheidet**: Sie erhebt die Zahlen, auf denen der offene Punkt 1 aus [`bam-suche.md`](bam-suche.md) §9 — **die Präfixsuche** — entschieden werden kann, einschließlich der Zahlen, die gegen sie sprechen. **M49‑1 beantwortet die tragende Frage mit Nein: Auffüllen und Präfixsuche schließen einander aus**, weil die Normalisierung aus Teil 2a vorn auffüllt und ein Präfixmuster ebenfalls vorn ankert — in **allen acht** prüfbaren Fällen findet die rohe Präfixfassung den aufgefüllten Originalwert **nicht**. Der Ausweg, die Nullen **ins Muster** zu ziehen, funktioniert ebenfalls in allen acht, und sein Preis ist gezählt: **drei bis sieben Fassungen je Begriff** statt der heutigen fünf **insgesamt** (M47) — bei 9006 sieben, und das bei *einem* von acht erlaubten Begriffen. Er kippt, sobald der Kern kurz ist: 9036 (Kern **ein** Zeichen, eine Verkürzung ist gar nicht möglich) und 9006 (drei Zeichen) erzeugen **über eine Million** Kandidatenzeilen, während sechs Typen mit langem Kern zwischen 1 und 93 bleiben. Nebenbefund, der die Kuratierung betrifft: **Zwei der acht Typen — 2001 und 9006 — stehen gar nicht in `bam_sollaenge`**, sind also durch die 95-Prozent-Regel gefallen; bei ihnen wäre Präfix sofort wirksam und sofort teuer. **M49‑2 beantwortet die Frage nach der Mindestlänge — ebenfalls mit Nein, und zwar endgültig:** Typlos über den Bestand fällt die schlimmste Trefferzahl von **1.332.180** (vier Zeichen) auf **234.159** (sechs) und **kann nie darunter fallen**, weil genau ein *exakter* Wert diese 234.159 Zeilen trägt (M33) — der Bösfall ist **kein Präfixproblem**, er steckt schon im exakten Bestand. Zwei zusätzliche Zeichen entwaffnen dabei **sechs Prozent** der gefährlichen Präfixe (1.731 → 1.624 ab 1.000 Treffern), bei 18,7-fach mehr Präfixen. Je Typ liegt die Grenze um **Faktor 3.969** auseinander (bei sechs Zeichen zwischen **59** bei 2000 und **234.159** bei 9014), und drei Typen haben einen Boden, den mehr Zeichen nicht senken (2001, 9015, 9014) — **eine Mindestlänge müsste also je Typ gelten und wäre nur durchsetzbar, wenn der Nutzer einen Typ gewählt hat, während die Suche typlos voreingestellt ist (M36); diese Spannung wird benannt und nicht aufgelöst.** Der Befund gegen die Erwartung: **Drei der zehn schlimmsten Präfixpaare gehören Typ 9019 „Bestellnummer vom Kunden"** — der Schnitt „gefährlich sind nur die Kennungsfelder" trennt nicht die gefährlichen von den **gesuchten** Feldern. **M49‑3** misst das **gebaute** Statement mit `LIKE` statt `IN` — gerendert aus `BamSucheRepository`, nur das Wertprädikat ersetzt, alles andere Zeichen für Zeichen unverändert: **Der Plan ändert sich nicht** (`MessageBAM_BAMValueOnly`, `range` statt `ref`), und der Optimierer steigt weiter über den **seltensten** Begriff ein, auch wenn er an fünfter Stelle steht — **kein `STRAIGHT_JOIN`**, Regel L15 belegt statt angenommen, Regel L7 über `NEXANS` und `IBIS`. Die Kosten: Normalfall **1,374 ms**, schlimmster Wert **1,823 s** über 30 Tage, vier Zeichen verkürzt **1,361 s** und **3,400 s** über ein Jahr — teuer, aber unter der Zeitgrenze. Der Befund, der in keiner Zeile des Auftrags stand und die Fachlichkeit trifft: **Schon der *vollständige* Wert als Präfix findet 23 statt 1** — M34s Bedingung „bei gleicher Trefferzahl" ist damit bei **keiner** Eingabelänge erfüllt, und E6s Zeile „8 (vollständig) → 1 Treffer" ist die exakte Zählung und keine `LIKE`-Zählung. **M49‑4** erhebt die Sonderzeichen über den Bestand: **`%` kommt kein einziges Mal vor** (und wäre als Kennzeichen frei), `_` steht in 2.696 und `*` in 1.738 Werten — jede Eingabe bräuchte mit `LIKE` ein `ESCAPE`, dieselbe Falle wie Regel Q1. **585 BAM-Werte tragen einen Doppelpunkt**, womit der ausdrücklich ungemessene Punkt aus §1 der Suche **bestätigt und nicht geschlossen** ist: Die Teilung am **ersten** Doppelpunkt war als Vorsicht gebaut und ist jetzt gemessen notwendig. Und **602.794 Werte (3,91 %) tragen ein folgendes Leerzeichen** — mit `=` harmlos (PAD SPACE), mit `LIKE` nicht, ein Trim auf der Eingabe würde also Pflicht (Gegenprobe ohne `LIKE` bestätigt die Zahl; leere Werte gibt es null). **Zwei Abweichungen vom Rahmen sind ausgewiesen**, dazu eine dritte an einem Statement: Die Bänder bei acht Zeichen sind bei 60 s **serverseitig abgebrochen** — der Abbruch steht als Ergebnis, weil die Frage dahinter aus dem Boden bei 234.159 ohnehin folgt; der Client läuft mit `--ssl-mode=DISABLED` statt des beauftragten `--skip-ssl`, das er nicht kennt, **um die Vergleichbarkeit mit M32 bis M48 nicht zu verlieren**; und die zwölf Erhebungen über den Bestand sind **einmalig** gemessen statt als beste von drei, weil ihr Gegenstand die Verteilung ist und nicht die Uhr — ihre Laufzeiten sind Obergrenzen und tragen keinen Befund. Der Vergleichsanker aus M47 reproduziert auf **105,9 %**, die Testkopie ist am **achten** Messtag byteidentisch, und vier weitere Kontrollen gehen auf (die Zeilenzahl 15.406.350 dreimal, die Kernlängen aus M43‑4, die Präfixzahlen 5.425/155.871 aus E6, die sechs Werte der Ankernachricht aus M42‑0). **Die Zahl, die den offenen Punkt entscheiden würde, fehlt weiterhin und fehlt nicht aus Nachlässigkeit:** *wie oft die exakte Suche heute leer ausgeht* — sie stünde nur in einem Suchprotokoll, das die Anwendung nicht führt. **Damit sind die Kosten der Präfixsuche beziffert und ihr Nutzen nicht**. **Achter Nachtrag (M50, 14.08.2026):** der Faktor‑8,5-Fall, den M49 offenließ — der schlimmste Präfix des Bestands (1.332.180 Zeilen) kostet über 30 Tage **3,851 s** und ist über ein Jahr **an der 60‑Sekunden-Grenze abgebrochen**; daraus der 30‑Tage-Deckel. Nebenbefund: **55.989 Werte tragen ein Komma** und waren damit unsuchbar (M50‑5). **Neunter Nachtrag (M51, 14.08.2026):** wo diese Kommas sitzen — **drei Typen von 62**, 96,62 % davon unter Typ 9003, alle drei bei **einem** Mandanten, und von den tragenden Suchtypen aus M39 nur 9018 mit **fünf** Werten. Dazu die Nachbarschaft: **0 von 1.503 `ProcessID`** tragen ein Komma. Die Runde entscheidet keinen Bau — sie beziffert, was die Behebung wert war |
| [`messungen-schritt6.md`](messungen-schritt6.md) | **vor Schritt 6** | M23 bis M27, M28 und **M30** gegen die Testkopie (10.08.2026), vor dem Bau der Verkettung — die Runde, die die Vorgabe `zwischenschritte=false` überprüft, deren Begründung nie gemessen war. **M23** findet beide Verkettungsspalten voll indiziert (`SourceMessageIDIDX`, `TargetMessageIDIDX`) und belegt, dass unbelegt immer `NULL` heißt und nie leerer String. **M24** dreht die bisherige Lesart um: Der Elternteil trägt `SPLITTED`, die Kinder `FINISHED` — der Produktions-Screenshot ist der **Normalfall**; die Breite reicht bis **3.048 Kinder**, womit der Plan „Liste oder einfacher Baum" ohne Breitengrenze nicht trägt; und drei Mandanten **ohne einen einzigen Zwischenschritt** (`IBIS`, `IBISGUS`, `ZAST`) haben trotzdem Ketten — Verkettung läuft also nicht über diese beiden Status. **M25** zeigt die Merge-Richtung (`MERGED` ist der **Eingang**, Grad 23 : 1, Ergebnis meist `EERP_RECEIVED` und damit bereits sichtbar) und dass die beiden Spalten einander **je Zeile ausschließen** — keine Symmetrie, beide Richtungen nötig (die daraus zunächst abgeleitete stärkere Aussage über die *Beziehungen* ist von M28‑1c widerlegt, siehe unten). **M26** ist der Befund, der die Vorgabe kippt: **96,9 %** der ausgeblendeten Wurzeln tragen BAM-Werte gegen **2,4 %** der gezeigten Kinder — kein Anzeige-, sondern ein **Suchproblem**, und zwar schon vor Schritt 7. **M27** entlastet: Quell- und Zielprozess führen ausnahmslos zum selben Mandanten (9.101 Zeilen), die Kette braucht keine Sonderbehandlung. Ergänzt um **E1** (die kuratierten Listenspalten sind auf der Vorgabe-Ansicht leer), **E2/E3** (die Kette ist mindestens vierstufig) und **E4** (die Flags `Source`/`Target` decken sich **exakt** mit der tatsächlichen Verkettung und sind der kostenlose Rückwärtsindex der ID-Spalten). **Block 2 vom selben Tag — M28**, die Messung, unter deren Vorbehalt die beiden Sparring-Entscheidungen zu Schritt 6 standen: **M28‑1** beziffert, was ein Stellungsprädikat kostet — es blendet **66,80 %** aller Zeilen aus gegen heute 31,15 %, bei `IBISGUS` **100 %** und bei `ZAST` 92,93 %, deren Liste damit leer wäre; die Entscheidung „Sichtbarkeit über die Stellung" wird dadurch **schwerer zu begründen, nicht leichter**. **M28‑1c** (ergänzt, weil der Plan nur vier der sechs Rollenpaare erhob) zeigt Doppelrollen bei **0,240 %** der Zeilen, bei `IBISGUS` 1,278 % und **nie mehr als zwei** je Zeile — die Rolle als Menge bleibt Pflicht — und findet dabei **33 Zeilen, die zugleich Split-Kind und Merge-Ergebnis sind**, womit ein aus M25‑2 verallgemeinerter Satz in `datenmodell.md` §7 widerlegt und dort geschärft ist. **M28‑2** misst den BAM-Typ je **Rolle** statt je Status: Auf den Split-Wurzeln von `NEXANS` sitzt Typ 9018 auf **92,26 %**, bei `IBIS` Typ 0 auf 92,59 % — der Satz in `nachrichtenliste.md` §6 („eine BAM-Spalte, die immer etwas zeigt, gibt es bei `NEXANS` nicht") gilt damit für die gezeigten Kinder (0,72 %), aber **nicht** für die ausgeblendeten Wurzeln; die Kuratierung 9006/9001 trifft mit 5,02 % und 11,79 % keine der beiden Rollen. **Block 3 vom selben Tag — M30**, erhoben vor Schritt 6, Teil 1, weil E5 für die Gegenrichtung nur den `EXPLAIN` hatte und keine Laufzeit: **M30‑1** misst alle sechs Richtungen einzeln in der Fassung, die der Code benutzt — ein Glied kostet **0,5 ms** (`const`, nicht `eq_ref`), die Seite der Kinder **26,0 ms** im Breitfall, die Zählung **19,8 ms** an der breitesten Wurzel des *gesamten* Bestands. Damit fällt die Entscheidung „genaue Zahl statt ‚mehr als 50'" an einer Zahl und nicht an einer Abwägung. Zwei Befunde ohne vorformulierte Zeile: Die Zählung ist **nicht** `Using index` — der Mandantenfilter braucht `Message.ProcessID`, das ist der gemessene Preis von Regel M5 —, und das **`LIMIT` begrenzt die Ausgabe, nicht die Arbeit**, weil das `filesort` alle Trefferzeilen sehen muss. **M30‑2** erhebt Breite und Selbstverweise über den **gesamten** Bestand (Regel L9, je rund 1,5 s, weil index-nah): bis **3.350** Kinder und **897** Merge-Eingänge, und die Grenze von 50 greift bei **11,38 %** der Merge-Ergebnisse gegen **1,07 %** der Wurzeln — beim Merge ist `weitereVorhanden` der Regelfall. **M30‑3** beziffert die Tiefe auf **genau vier Glieder** (Stufe fünf ist leer) und schließt damit zugleich einen **Zyklus** aus; **M30‑4** zieht die vollständige Aufstellung der Rollenkombinationen über beide Fenster nach und beantwortet die Frage nach dem **sechsten Paar** (`Wurzel + Eingang`: gemessen null, in M28‑1c bereits erhoben); **M30‑5** prüft, dass der Mandantenfilter die Zählung nicht verändert (3.048/3.048, 749/749) — die Zusicherung bleibt trotzdem. Je mit Statement, `EXPLAIN` und Laufzeit; die Deutungen standen **vor** der Messung fest, und die **acht** Stellen, an denen der gemessene Befund in keine vorformulierte Zeile passt, sind als solche ausgewiesen. **Block 4 vom 11.08.2026 — M31**, die Nacharbeit zu Teil 2b: **M31‑1** zählt, wie viele **Ansichten** der zickzackende Aufstieg verzerrt — die 33 aus M30‑4 zählen **Knoten**, und an ihnen hängen **390** Merge-Eingänge (1,01 %, im Bestand 7.935 an 795 Knoten); **M31‑2** misst den Spiegelfall mit **null** über 104.538 Split-Kinder und fünf Mandanten und findet die Voraussetzung dafür (`Wurzel + Eingang`) **vier** Mal im Bestand, bei einem Mandanten, der in keinem Fenster vorkommt; die Stufenverteilung belegt, dass ein Beziehungswechsel **nie tiefer als Stufe −2** reicht. **M31‑3** kann den Takt von `MatchInterchange` **nicht belegen**: `information_schema.EVENTS` ist leer, aber nachweislich aus Rechtegründen (`SHOW EVENTS` → `ERROR 1044`), und `COMMIT_RECEIVED` ballt sich mit 0,597 genauso wie `MERGED` mit 0,601 — ein Status, den kein Event schreibt. Der Belegvermerk sagt, warum diese Messung den Takt **zeigen, aber nicht ausschließen** kann. Konsequenz: „stündlich" ist in [`PROJEKTBESCHREIBUNG.md`](PROJEKTBESCHREIBUNG.md) §3.3 als **ungedeckt gekennzeichnet**, nicht gestrichen |
| [`messungen-schritt5.md`](messungen-schritt5.md) | **vor Schritt 5** | M14 bis M17 gegen die Testkopie (07.08.2026), vor dem Bau des Nachrichtendetails: **M14** Spalten, Indizes und Größe von `MessageAction`, `MessageProperty` und `Service` — findet die undokumentierten Spalten `MessageAction.SOSID` und `MessageAction.SOSActionID` und belegt die Präfix-Indizes über 50 Zeichen. **M15** entscheidet, ob die Zeitleiste Klartext trägt: der Join über `(MessageAction.SOSID, MessageAction.SOSActionID)` ist mit **null Abweichungen** beim Vergleich des ausgeführten mit dem geplanten Baustein belegt, 71 bis 78 % der echten Schritte bekommen einen Namen — **die im Plantext beauftragte Zuordnungstabelle entfällt**. **M16** Gestalt der Schrittfolge: höchstens sieben Aktionen, also keine Obergrenze nötig; eine wartende Nachricht steht **zwischen** zwei Schritten, und der Hänger ist an `MessageActionEnd IS NULL` erkennbar (95 Fälle im Gesamtbestand). **M17** `MessageProperty`: 101 Namen statt der zehn dokumentierten, rund 22,6 statt zehn Zeilen je Nachricht, **595 Byte je Nachricht** statt der befürchteten Megabyte — und die Eigenschaften verteilen sich über die Schritte, getrennt in `Message.*` (Metadaten auf Schritt `0`) und `<Dienst>.*` (Protokoll am jeweiligen Schritt). Je mit Statement, Ergebnis, `EXPLAIN` und Laufzeit; dazu die Widersprüche zur Dokumentation und die daraus **offenen Fragen**. **Nachtrag vom 07.08.2026 (S1, M18 bis M22)** — diese Abschnitte ziehen ihre Schlussfolgerung ausdrücklich: **S1** legt das Kriterium des Metadaten-Schritts fest, **M18** zeigt, dass die Namenslücke nicht gestreut ist, sondern auf drei Kennungen sitzt, **M19** beziffert eine Zuordnungstabelle auf **eine Zeile für 90 %** und findet einen zweiten Weg ohne jede gepflegte Zeile, **M20** widerlegt die Vermutung zu den 43,9 % aus M13 und benennt die wahre Ursache (die Ablaufdefinition nummeriert nicht lückenlos), **M21** misst, wie oft ein Gerüst aus geplanten Schritten trägt, **M22** trennt den wiederkehrenden Status von dem einen Vorgang mit offenen Schritten. **Nachtrag vom 10.08.2026 (M29)** — die Nummer springt, weil M23 bis M28 an [`messungen-schritt6.md`](messungen-schritt6.md) vergeben sind und die Zählung **projektweit** läuft und nicht je Datei: Worauf zeigt `Message.SOSActionID` bei wartenden Nachrichten? Über **alle 538** auf den **zuletzt gelaufenen** Schritt — den mit `WAITUNTIL\|…\|SUSPEND` —, **null Mal** auf einen kommenden. Damit wird der Wartezustand in `WARTET_IN` und `WARTET_VOR` aufgeteilt, und die Oberfläche hört auf, zwei gelieferte Kennungen selbst zu vergleichen. Mit Vorprüfung, dass die Ordnung der Zeitleiste und `MAX(MessageActionID)` dieselbe Zeile treffen (538 von 538), und mit dem ausdrücklichen Hinweis, dass `n = 538` die Zahl der **Zeilen** ist und nicht die der **Fälle**: eine Gestalt, ein Ablauf, sieben Tage |
| [`messungen-schritt4.md`](messungen-schritt4.md) | **vor Schritt 4** | M0 bis M7 gegen die Testkopie (01.08.2026): Datenstand, Spalten und Indizes, `MessageTimeout`, Nachrichten je Mandant, View gegen `EXISTS`, `SOSID`, `SPLITTED`/`MERGED`, BAM-Konfiguration — je mit Statement, Ergebnis, `EXPLAIN` und Laufzeit. Dazu die Auffälligkeiten und die daraus **offenen Fragen**. Reine Erhebung, keine Entscheidungen. Nachgetragen: M8/M9 (die beiden Fragen, die sie entscheiden), L1 bis L13 zu den Statements des Listen-Endpunkts — darunter M10 und L11, die beide vorgesehenen Umbauten des Freitextfilters widerlegen, und **L13**, aus dem seine Fenstergrenze folgt. Zur Nachbesserung (06.08.2026): **M11** BAM-Abdeckung je Typ (Vorarbeit für Schritt 7), **M12** Zwischenschritte je Mandant, **M13** `SOSAction` — die Messung, die entscheidet, ob der aktuelle Schritt anzeigbar ist |

---

## Nachtrag 19.08.2026 — das Verzeichnis ist gegen `docs/` geprüft worden

**Anlass war ein offener Punkt, dessen Voraussetzung nicht mehr zutraf.**
[`rohdaten-backend.md`](rohdaten-backend.md) §11, Punkt 1 führt „`docs/README.md` fehlen zwei
Einträge — `rohdaten-backend.md` und `START-LOKAL.md`", gesperrt durch den Auftrag zu Schritt 8. Die
Sperre gilt nicht mehr. Geprüft ist deshalb **nicht die Annahme, sondern der Bestand**: jede
`*.md`-Datei in `docs/` gegen die Verweise in dieser Datei.

**Befund: Die beiden genannten Einträge fehlen nicht mehr.** `START-LOKAL.md` steht unter
„Grundlagen", `rohdaten.md`, `rohdaten-backend.md` und `rohdaten-frontend.md` stehen unter
„Features"; die Vorschauzeile zu `rohdaten-download.md` ist durchgestrichen und mit dem Hinweis auf
die drei entstandenen Dateien versehen. Alle vier Einträge sind mit dem Commit `8400952`
(18.08.2026) hinzugekommen — also nach dem Stand, den jener offene Punkt beschreibt. **Er ist damit
erledigt, steht in seiner Datei aber weiterhin als offen**; die Kennzeichnung dort ist nicht
Gegenstand dieses Laufs und **gemeldet, nicht behoben**.

**Gefehlt hat eine andere Datei:** [`messungen-schritt8-auftrag.md`](messungen-schritt8-auftrag.md),
als einzige der 27 Dateien, die dieses Verzeichnis neben sich selbst führt (`docs/` enthält 28
`*.md`-Dateien einschließlich dieser). Sie ist unter „Erhebungen" nachgetragen. Die Zeile steht
neben der Messdatei, zu der sie gehört, und benennt ausdrücklich, dass sie keine Erhebung ist.

> **Zwei Dinge, die dabei aufgefallen sind und hier stehen, statt behoben zu werden.**
>
> 1. **Drei Tabellen dieser Datei sind durch Leerzeilen aufgetrennt.** Vor der Zeile zu `bam-suche.md`
>    im Verzeichnis „Angelegt" und vor den beiden letzten Zeilen der Tabelle „Offene Sichtprüfungen"
>    steht je eine Leerzeile. Markdown beendet eine Tabelle an der Leerzeile — die vier Zeilen zu
>    `bam-suche.md`, `rohdaten.md`, `rohdaten-backend.md` und `rohdaten-frontend.md` erscheinen
>    deshalb als **Text und nicht als Tabellenzeilen**, ebenso die Sichtprüfungen zu „7, Teil 4" und
>    „8 — Rohdaten und Protokolle". Der Inhalt steht da, die Darstellung trägt ihn nicht. Das ist
>    älter als dieser Lauf und ohne datierten Vermerk nicht zu beheben, weil es keine alte *Fassung*
>    gibt, die zu zitieren wäre — nur ein Zeichen zu viel.
> 2. **Die Beschreibung zu [`messungen-schritt8.md`](messungen-schritt8.md) ist überholt.** Sie führt
>    „Teil B ist an seinem ersten Lauf stehen geblieben" und „M60, M61, M63, M65, M66, M67 sind nicht
>    gefahren". Der Commit `ee19e02` (18.08.2026) heißt „teil a und b vollständig", und
>    [`rohdaten.md`](rohdaten.md) §2 beruft sich auf M60, M61, M63, M64, M65, M66, M67, M68 und M71
>    mit konkreten Zahlen. Die Zelle ist damit auf einem Stand vom 14.08.2026. **Nachzuziehen, aber
>    nicht in diesem Lauf** — er ändert vier Dateien plus diese, und eine fünfhundert Wörter lange
>    Zelle neu zu fassen ist keine Nebenbei-Korrektur.
