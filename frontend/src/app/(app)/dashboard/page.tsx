import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dashboard" };

/** Platzhalter. Die Kennzahlen entstehen in Schritt 10 — ausschliesslich aus message_rollup. */
export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
    </main>
  );
}
